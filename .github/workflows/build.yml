name: Build Cross-Platform

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}

    # Windows-only: the old macos/ubuntu jobs never shipped anything and the ubuntu job
    # failed at `npm ci` (usb native module needs libudev.h, absent on the runner), which
    # fail-fast-cancelled the Windows build before it ran. Windows compiles usb against
    # the WinUSB backend (no udev), so it builds standalone.
    strategy:
      matrix:
        os: [windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npm run build

      - name: Build Electron app (Windows)
        # x64 only — the ia32 leg forced a 32-bit native-module compile; the old
        # `npm run electron:dist:win` script was a no-op echo stub.
        run: npx electron-builder --win --x64 --publish=never

      - name: Upload artifacts (Windows)
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist-electron/*.exe
          if-no-files-found: error
