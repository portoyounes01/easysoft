# Domain Docs

This is a single-context repository.

Before engineering exploration, read `CONTEXT.md` and relevant records under `docs/adr/` when they exist. Proceed silently when either is absent.

Use terminology defined in `CONTEXT.md`. Surface any conflict with an existing ADR instead of silently overriding it.
