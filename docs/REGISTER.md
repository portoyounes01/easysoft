# Multi-Tenant Conversion — Living Register

**Purpose:** one place to track every blocker, assumption, caveat, trade-off, and
deferral so nothing is silently dropped. Companion to `docs/PHASE-STATUS.md` (per-phase
status), `docs/multi-tenant-plan.md` (design), `docs/update-policy.md` (fleet updates).

**How to use:** each item has a stable ID (e.g. `B2`, `A5`). Reference it in commits/PRs
("closes A2"). Update **Status** + **Updated** when it changes. Add new rows; don't renumber.

**Status legend:** `OPEN` (needs action) · `RESOLVED` (done + verified) · `ACCEPTED`
(deliberate, no action planned) · `GATED` (waiting on an external party).
**Owner:** who must act — `user`, `me` (assistant/eng), `fiskaly`, `AT` (tax authority).

Last reviewed: **2026-07-24**.

---

## 1. Blockers (gate further progress)

| ID | Item | Detail | Owner | Status | Unblock action |
|----|------|--------|-------|--------|----------------|
| **B1** | fiskaly TEST credentials | Old `.env` creds returned `invalid_grant` (name, not key). **User supplied correct keys 2026-07-06 → auth verified (HTTP 200).** | user | ✅ RESOLVED (2026-07-06) | Done. |
| **B2** | Deploy `pos-checkout` **function** to **prod (EasySoft)** | `supabase db push` / function deploy to prod is correctly gated by the deploy classifier; needs explicit human go-ahead. ✅ **CORRECTION 2026-07-27:** the migration half of this row was stale — `20260716000000` **is applied** on prod (verified in the remote ledger, see D-MIG1). Only the `pos-checkout` **edge function** deploy remains unverified; the ledger says nothing about function deploys. | user | 🟡 PARTIAL — migration applied, function deploy unverified | Confirm/deploy the `pos-checkout` function (`supabase functions deploy pos-checkout`). |
| **B3** | **Staging** DB credentials | Only EasySoft's DB password is in `.env`; staging (`mubdnwmbvdutqzzprjdp`) password is unknown, so I can't test on staging first. | user | OPEN | Provide staging DB password, or authorize a staging deploy. |
| **B4** | Records-response contract unverified | The `POST /records` (INTENTION→TRANSACTION) **response** shape (ATCUD/hash/qr/certificate field paths, number authority) is not yet observed. Needs one real TEST issuance, which needs a provisioned taxpayer→location→system tree (none exist under the org yet). | me | 🟡 SCHEMA ✅ (2026-07-19, B16) / RUNTIME OPEN | Field paths now VERIFIED from the official OpenAPI (`compliance.data`=ATCUD, `compliance.qr_code`, `compliance.signature_hash`, state/mode semantics) and code fixed — see `docs/fiskaly-pt-contract-capture.md`. Runtime VALUES still need one real TEST issuance (account enablement, B14). |
| **B5** | AT software-certificate confirmation | "fiskaly" is absent from AT's public certified-programs list; the certificate number prints on every document. **Blocks any LIVE tenant / contracting.** | fiskaly | GATED | Get written confirmation of the AT certificate number from fiskaly. |
| **B6** | fiskaly written answers (plan §13) | Buyer-NIF policy on FS/FR, document-number allocation authority, LIVE doc-type coverage (FT/RG-RC/GT-GR), QES-for-PDF vs the 2027-01-01 mandate, pricing, SAF-T submission responsibility. | fiskaly | GATED | Email fiskaly support/sales; record answers here. |
| **B7** | fiskaly layout approval | fiskaly must approve each document layout in writing before production (indemnity on tenant-modified layouts). | fiskaly | GATED | Submit layouts for approval before first LIVE tenant. |
| **B8** | Real 2nd business + PWA (Phase 5) | Identity/RLS already support a direct-PostgREST PWA (proven by the isolation probe); the actual PWA build + a real second tenant are out of scope for the current work. | user | OPEN | Onboard a real second business; scope the PWA build. |
| **B9** | Dashboard configs | S6: disable public signup (Auth → "Allow new users to sign up"). JWT expiry → 15 min (currently 3600s). CLI mgmt token is in the macOS keychain, not scriptable from here. | user | OPEN | Toggle in Supabase dashboard (both prod + staging). |
| **B10** | Vercel hygiene (S13) | Drop unused `VITE_FISCAL_RSA_PRIVATE_KEY_PEM` env var (never used in prod; we use fiskaly). | user | OPEN | Remove from Vercel project env. |
| **B11** | Prod CORS allowlist (S21) | Edge functions currently allow `*`. Needs the exact prod origin set (Electron `app://pos` + real Vercel domain) to lock down without opaque breakage. | user | OPEN | Provide exact prod origins; I'll set the allowlist. |
| **B12** | **fiskaly account has NO SIGN PT (Portugal) — verified in-dashboard 2026-07-06** | Logged into the account (user CHRIT). **Classic `dashboard.fiskaly.com`** org `group1` has: Germany SIGN DE, Germany DSFinV-K, Austria SIGN AT, **Spain SIGN ES**, fiskaly Receipt — **no Portugal**. That's why the `.env` key is a Spain key (`azp: sign-es`). **New `hub.fiskaly.com`** account `my_account` (org `bf68bcbd…`) has only **RECEIPT + SAFE** services enabled. | user + fiskaly | ⚰️ SUPERSEDED (2026-07-08) | **fiskaly email (Eduardo Jacobo, 2026-07-06): Portugal + Belgium will be served by the UAPI ("API Unificada"), NOT SIGN PT — and Spain (SIGN ES) is "la que más os urge".** Do NOT send `docs/fiskaly-signpt-request.md` (kept for history). Replaced by B13/B14. |
| **B13** | **fiskaly contract e-signature (Dropbox)** | Eduardo sent the document via Dropbox for electronic signature ("para que puedas proceder con la firma electrónica", 2026-07-06). Likely gates LIVE access / commercial terms. | user | OPEN | Sign the Dropbox document (review terms first — pricing, indemnity, per-country coverage). Record what was signed here. |
| **B14** | **Portugal + Belgium fiscalization = fiskaly UAPI** — RESEARCHED 2026-07-08 | **Naming reconciled: SIGN PT *is* the UAPI's Portugal service** (launched 2026-05-21 on `api.fiskaly.com`, "enabled for productive use" per X-Api-Version 2026-06-01: series+ATCUD, signing/chaining, QR, SAF-T 1.04_01, AT web services; QES mandatory 2027-01-01). The parked `pos-checkout` already targets the right host family. **BUT: (1) ⚠️ fiskaly is ABSENT from AT's public certified-programs register** (verified through cert #3095, 2026-06-15; competitor fiskaltrust holds #3083, positive controls Vendus/Cegid/Moloni all present) — Portaria 363/2010 requires the cert number printed on every document, so legal PT issuance via fiskaly is **unverifiable today** (elevates B5 with hard evidence); **(2) SIGN PT not enabled on org group1. Belgium: NO POS fiscalization product exists — only E-INVOICE BE (Peppol B2B, Beta).** | fiskaly | GATED | `local_at` stays PT primary (confirmed). Reopen only when: fiskaly's AT cert number verified in the register + SIGN PT enabled on our account → then run a PT ES-0-style contract pass. Belgium parked. Questions #1/#2/#10 in `docs/fiskaly-strategy-brief.md` are email-ready. |
| **B15** | **Spain / SIGN ES — ACTIVATED** (was D11 "not now") | ES is the urgent track. **TEST auth verified 2026-07-08**; org `7a0289c1…` blank. **Full API contract mapped from the OpenAPI (v1.24.2, `live.es.sign.fiskaly.com/api/v1/en/oas.yaml`)**: provisioning `PUT /taxpayer` (NIF+territory; effectively immutable) → social-collaboration agreement (legal rep signs PAdES; lets fiskaly submit to AEAT) → `PUT /signers/{uuid}` → `PUT /clients/{uuid}` (one per till); issuance `PUT /clients/{cid}/invoices/{iid}` (client UUID; **we assign numbers** → `allocate_fiscal_number` reusable; decimal STRINGS; per-line VAT `system` discriminator, recargo via `additional_vat`); response carries AEAT QR (base64) + mandatory legend; **AEAT registration is ASYNC (~60–70s, poll-only, no webhooks)** — rejections are HTTP 200 + `REQUIRES_CORRECTION`; corrections = CORRECTING (R1–R4) / REMEDY / CANCELLED. **Regulatory: taxpayer deadlines 2027-01-01 (societies) / 2027-07-01 (autónomos) — runway; but the VENDOR deadline passed 2025-07-29** (art 201 bis LGT: selling a non-adapted SIF into Spain = up to €150k/yr exposure + we owe our own declaración responsable). TEST has magic NIFs T00000001–T00000004. | me + user | IN PROGRESS — **ES-0 ✅ DONE (2026-07-08)** | Brief + phased plan: **`docs/fiskaly-strategy-brief.md`**. Decisions taken (user): magic-NIF TEST start, Veri*factu-only v1, rectificativas in v1. **ES-0 executed end-to-end**: managed org `EasySoft ES Pruebas TEST` (`8513aab6…`) created via Management API + user-minted key; taxpayer→signer→client provisioned; **SIMPLIFIED invoice ISSUED** (QR PNG + `QR tributario:|VERI*FACTU` legend + AEAT-preprod validation URL); async verdict contract observed (PENDING→`REQUIRES_INSPECTION` + `validations[]` AEAT codes — magic NIF invalid as ISSUER); idempotency measured (same UUID+same body=echo, different body=409); **CORRECTING (rectificativa) verified** (nested-invoice wrapper, own series); taxpayer immutable once resources enabled. Full capture: **`docs/es-sign-contract-capture.md`**. **ES-1 ✅ DONE (2026-07-08, `20260727000000` deployed)**: country-aware `tenants.nif` CHECK (PT digits / ES alnum; +ISO country CHECK), issuer `'sign_es'` allowed, `tenant_fiscal_config.sign_es_org_id/signer_id`, `devices.sign_es_client_id_test/live`, `fiscal_documents` verifactu columns (registration/cancellation states, invoice_kind, AEAT URL, legend) + PENDING-reconciliation index; client `ES_IVA_RATES` + `ivaRatesForCountry` (UI wiring = ES-3). **ES-2 ✅ CODE DONE + DEPLOYED (2026-07-08, `c6be9a6`)**: `pos-checkout-es` (SIGN ES issuer) + `reconcile-verifactu` (async AEAT-verdict poller, per-minute cron `20260728000000`, null-safe) + `VERIFACTU_REJECTED` critical alert type (`20260728001000`) wired into the P3 push pipeline + `scripts/es-sign-onboard.mjs` (per-tenant provisioner). **Adversarially reviewed pre-deploy (18 Opus agents, 15 findings) — all fixed**: number reserved-once/reused-every-retry + 409-recovery (no deadlock/burned-number/lost-archive-row on a post-issue failure); reconcile alert plain-insert (uq_notif_once is a PARTIAL index, ON CONFLICT 42P10'd); money validation before allocation; tenant-scoped ledger; sweep 7-day bound + negative token cache; COMPLETE needs valid NIF; issue-time rejection alerts immediately. Gates smoke-tested (401s). ⏳ **ACTIVATION (manual, classifier-gated)**: (1) onboard a tenant via `es-sign-onboard.mjs` (needs a managed-org key mint + the emitted SQL run); (2) seed Vault `reconcile_verifactu_fn_url` + `reconcile_verifactu_shared_secret` and edge secret `RECONCILE_SHARED_SECRET` for the reconcile cron (same shape as notif-push); (3) point a till's `tenant_fiscal_config.issuer='sign_es'` + provision. **ES-3 + ES-4 ✅ CODE DONE + DEPLOYED (2026-07-08, `9540ef4`)**: `signEsFiscalIssuer.ts` (cart→SIGN ES mapping, buyer-NIF→COMPLETE, items-sum guard) wired into `checkoutOrchestrator` + `creditNoteCheckout`; `FiscalProvider`/settings issuer union + Settings selector gain `sign_es`; receipt shows the `VERI*FACTU` legend + AEAT QR (`ThermalReceipt.verifactuLegend`, live + reprint paths). ES-4 rectificativas: `pos-checkout-es` CORRECTING (ES-0-verified wrapper) + `issueSignEsCreditNoteForTransaction` (sustitución a 0). **Onboarding is now IN-APP, not a CLI** (per user: a technician/owner self-installs): `provision-sign-es` edge fn does the whole chain from one owner/admin call (managed org→key→taxpayer→signer→clients→persist); `es-sign-onboard.mjs` demoted to dev tool. Gates smoke-tested. ⏳ **ACTIVATION (platform operator, once)**: set edge secrets `FISKALY_ROOT_API_KEY_TEST/_SECRET_TEST` + `FISKALY_ROOT_ORG_ID` (group1 `7a0289c1…`) so `provision-sign-es` can mint managed-org keys; + the reconcile-cron Vault secrets (B15). Then: owner opens the (future) in-app SIGN ES onboarding form → provisioned. ⏳ **UI follow-ups**: ✅ operating-country setting + full ES UI + country-aware tax pickers/NIF/postal/locale shipped 2026-07-08 (**D-ES2**); remaining = the in-app onboarding FORM that calls `provision-sign-es` (backend ready) + the D-ES2 polish list (product-rate migration on flip, phone `+34`, provincia, ES doc-title, timezone). ⚠ COMPLETE (buyer-NIF) + discounted-line rounding not yet issued in TEST — verify before first LIVE such sale. |
| **B16** | **PT / SIGN PT contract VERIFIED at schema level + all PT fiskaly code fixed — 2026-07-19** | User challenged the "SIGN PT" naming ("doesn't exist; PT is on the unified API"). 5-agent research pass (official OpenAPI downloads + live schema-echo probes): **"SIGN PT" IS the real current product name** (launched 2026-05-21, `fiskaly.com/signpt`, spec `info.title`) **AND it runs on the unified API** — shared `test/live.api.fiskaly.com` hosts, unlike SIGN ES (separate Specialized API; fiskaly states no migration planned). Full verified contract: **`docs/fiskaly-pt-contract-capture.md`** (spec `workspace.fiskaly.com/specs/fiskaly.sign-pt.2026-06-01.yaml`). Corrections found & FIXED: (1) legacy `fiskaly-fiscal` proxy was fictional end-to-end (flat auth body schema-rejected by the real API; token path wrong; `PUT /systems/{id}/records/{rid}` 404s — a SIGN DE shape; missing required `X-Idempotency-Key` on `/tokens`; invented SAF-T endpoint) → rewritten to the verified transport + INTENTION→TRANSACTION flow + real `/files` AUDIT SAF-T; (2) `pos-checkout` response parsing guessed wrong paths (ATCUD is `compliance.data`, QR `compliance.qr_code`, printed hash `compliance.signature_hash`), treated HTTP 200 as success although **REJECTED/FAILED arrive as 200**, and used the taxpayer-id placeholder as System id → all fixed (now `devices.fiskaly_system_id_<env>` + payload validation before number allocation + INVOICE requires client-built recipients); (3) client issuer assumed fiskaly assigns numbers — **spec: the TAXPAYER generates document numbers/series** (falsifies A3) → client-side numbering added (series label + Dexie last-sequential per chain scope); all money values now decimal STRINGS per spec. | me | SCHEMA ✅ / RUNTIME OPEN | ⚠️ RUNTIME still unverified — SIGN PT not enabled on org group1; no authenticated call has ever run. Open items (capture doc §6): VAT-code mapping 23/13/6→STANDARD/REDUCED_1/REDUCED_2 **ASSUMED**, M-code↔CAUSE_n mapping, OTHER("CARD") payment acceptability, PROCESSING→FINISHED timing, rounding tolerance, series-registration mechanics. `local_at` stays PT primary; B14 gates unchanged (AT cert number + account enablement + a PT ES-0-style TEST pass before any real use). **Post-fix adversarial review (31 agents, 26 confirmed findings incl. 3 critical) applied same day**: spec-invalid `product`/`address.line` shapes fixed; incomplete records (no ATCUD/QR/signature_hash) now FAIL instead of persisting as 'issued'; retries reuse the reserved number on pending AND failed attempts (deterministic keys replay instead of 422); client converges identical retries via the attempt ledger and skips possibly-burned numbers on changed carts; cent-consistent entry/breakdown/totals sums; global discounts loudly blocked; `/files` create-replay assumption removed. |

---

## 2. Assumptions (believed true; verify before relying)

| ID | Assumption | Basis | Status | Note |
|----|-----------|-------|--------|------|
| **A1** | Token = `content.authentication.bearer`, ~24h JWT, `expires_at` present | Verified against `test.api.fiskaly.com` 2026-07-06 | ✅ RESOLVED | `pos-checkout` fixed to read this (was `access_token`, would have failed every checkout). |
| **A2** | INTENTION returns an `id`; TRANSACTION response carries ATCUD/hash/qr/certificate | Official OpenAPI 2026-06-01 (B16) | ✅ RESOLVED (schema, 2026-07-19) | Verified: envelope `{content:{id, state, mode, …}}`; ATCUD=`compliance.data`, QR=`compliance.qr_code`, printed hash=`compliance.signature_hash`, chain=`journal.signature`. Parsing fixed accordingly. Runtime values pending B4. |
| **A3** | fiskaly may override the client `document.number` (returned number authoritative) | plan §13 #8; request side confirms client MUST send it | ❌ FALSIFIED (2026-07-19, B16) | Spec: `DocumentNumber` "must be generated by the taxpayer"; the response does not return an authoritative number (operation echo is opaque JsonData). Code persists OUR number. |
| **A4** | fiskaly auto-registers the series and returns the ATCUD | SIGN IT guide wording ("assigned progressive number") | OPEN | SIGN PT docs say it handles "registration and management of document series with the AT", but automatic-on-first-use vs explicit is unverified (capture doc §6.7). Confirm at the first TEST pass. |
| **A5** | `systemId` = the fiskaly **System (till) id** | plan §3 hierarchy; spec `RecordSystem` | ✅ RESOLVED (code, 2026-07-19) | `pos-checkout` now reads `devices.fiskaly_system_id_test/live` (placeholder repointed); fails `fiscal_system_unprovisioned` when absent. Provisioning (D10) must populate these columns. |
| **A6** | The fiskaly account/org is the **SIGN PT** product | Probe resource model matched PT (UNIT/BRANCH/FISCAL_DEVICE) | ❌ FALSIFIED — it's **SIGN ES** | The TEST key's token has `azp: sign-es` (= the OAuth client that issued it is fiskaly's **Spain** product); its `organization`+`subject` ids **404** on the SIGN PT surface; PT lists are empty. High confidence this is a **Spain (SIGN ES)** key. See **B12**. |
| **A7** | Validated buyer NIF requires an **INVOICE**; RECEIPT only has a free `customer.code` | Verified (request-side schema) | ✅ RESOLVED | Product rule needed: when to escalate FS/FR → FT (see D-note). 2026-07-19 (B16): `pos-checkout` + client no longer auto-upgrade NIF→INVOICE (the old path fabricated schema-invalid recipients and would have 400'd); NIF rides `customer.code` on receipts. ⚠️ Whether `customer.code` feeds SAF-T CustomerTaxID is a B6 question for fiskaly. |
| **A8** | Tenant isolation holds for the (future) PWA direct-PostgREST path | Proven via SQL probes w/ simulated JWT claims | ACCEPTED (caveat) | Equivalent for RLS correctness; the PWA path itself is unexercised (no PWA yet). |

---

## 3. Caveats (known limits of what's shipped)

| ID | Caveat | Impact | Status |
|----|--------|--------|--------|
| **C1** | `pos-checkout` **never deployed or run** | Request contract is verified + retry-safe by construction, but no end-to-end execution has happened. | OPEN (B2/B3/B4) |
| **C2** | App not re-smoke-tested in-browser after Phase 3 | Low risk (RPC-heavy; RPCs bypass RLS; the 2 direct selects are tenant-scoped and own-tenant reads proven). | 🟡 PARTLY CLEARED (2026-07-06): browser-core boots + login screen renders clean after the D6 fix. Electron/paired-till checkout path still user-tested (you own Electron). |
| **C3** | Demo seed data on EasySoft (SYS001/ADM001 etc.) | Will be wiped/replaced at real tenant provisioning; don't treat as real. | ACCEPTED |
| **C4** | Client still uses its existing issuer path | The POS UI is **not** wired to `pos-checkout` yet (deliberate — see T6). | OPEN (after B4) |
| **C5** | `employee_credentials` + `tenant_fiscal_secrets` are plaintext-at-rest | Access-locked (RLS on, no policy, service_role only) but not encrypted. | see D3 |

---

## 4. Trade-offs (deliberate choices with a cost)

| ID | Choice | Cost accepted | Why |
|----|--------|---------------|-----|
| **T1** | v1 is **online-required** (no offline queue) | A network/fiskaly/backend outage stops selling. | User decision D1; simplicity + fiskaly-primary; fail-closed on selling (update-policy §7). |
| **T2** | Allocate `document.number` **before** the fiskaly call | A fiskaly failure leaves a **number gap**. | PT series tolerate explainable gaps; `fiscal_issue_attempts` records every spent number; a reconciler (D8) can void gaps if AT requires. Alternative (allocate after) risks duplicate numbers under concurrency. |
| **T3** | Secrets plaintext-at-rest, access-locked | Weaker than encryption if the DB is exfiltrated with service_role. | Structural home first; Vault is a follow-up (D3). Real threat (client/anon access) already blocked. |
| **T4** | Storage bucket stays **public** for product images | Product photos are cross-tenant enumerable (low severity). | Private cutover is invasive (D2); no PII in product images. |
| **T5** | One series per `(tenant, env, doc_type)` in v1 | No per-store/per-device series segmentation yet. | Simpler; matches single-till-per-store pilots; can split later without data change. |
| **T6** | Don't wire the client to `pos-checkout` until it's verified E2E | Checkout still on the old path for now. | Shipping the client against an unverified **response** shape would be "unreviewed". Wire after B4. |
| **T7** | Legacy fiscal issuers hardened (not yet deleted) | Deleting `fiskaly-/vendus-/invoicexpress-fiscal` now would break any till still dispatching to them (client not yet on `pos-checkout`, C4). | ✅ HARDENED (`7942283`, P0 §4.2 L2): each now `verify_jwt=true` + rejects non-`device` sessions in-body, closing the "any JWT issues fiscal" hole. Full deletion + provider-env-var removal = **D30** (coordinated cutover). Inert until deployed (B2/B3). |

---

## 5. Deferrals (postponed, with rationale + how to finish)

| ID | Deferred | Rationale | How to finish |
|----|----------|-----------|---------------|
| **D1** | FORCE ROW LEVEL SECURITY | Would subject SECURITY-DEFINER RPCs (owner context) to `TO authenticated` policies → they'd match nothing and break. `service_role` bypasses FORCE anyway; the real threat (hostile authenticated session) is already blocked + proven. | Rewrite policies `TO public USING(tenant_id=app.tenant_id())`, `ALTER TABLE … FORCE RLS`, re-run isolation + RPC-still-works probes on staging first. |
| **D2** | Storage Ring 4 (private images, tenant paths, signed URLs) | Flipping to private breaks every existing image; needs path-prefixing, signed-URL reads, object migration, browser testing. Low-severity leak. | Dual-read window → move objects to `{tenant_id}/…` → rewrite `products.image_url` → private flip → `storage.objects` path-prefix policies → signed-URL reads. |
| **D3** | Vault (pgsodium) encryption of `tenant_fiscal_secrets` (+ `employee_credentials`) | Table is the access-locked structural home; encryption-at-rest is additive. | Move secret columns into Vault; update the definer/service paths to decrypt. plan §4.6. |
| **D4** | `ConnectivityGate` UX polish | **Partially covered:** fiscal issuers already gate on `isOnline && isSupabaseOnline`, so an offline checkout already fails. Remaining is UX only (disabled Pay button + banner vs a failed attempt). Needs browser testing. | Gate the Pay button + show an offline banner in `POS.tsx`; verify in-app. |
| **D5** | Post-pair bootstrap (pull roster/catalog after pairing) | Client change; needs browser verification. | After successful pairing, trigger the initial delta sync before first use. |
| **D6** | Authenticated-sync fail-closed graceful handling | Anon table grants are revoked, so anon sync already fails — needs graceful handling, not error-spam. | ✅ DONE (2026-07-06, `12efaba`): `hasAuthSession()` guards in `syncManager.fullSync/bootstrap` + `employeeService.performSync` skip sync until authed. Verified in-browser: no `upsert_employees` 401 spam, no misleading "could not reach server" banner. |
| **D7** | Negative authorization tests (image/purchase/HR) | Need reliable in-app/browser verification (sandbox↔prod REST is flaky here). | Add tests asserting cross-tenant + unauthenticated writes are denied. |
| **D8** | Fiscal number gap-reconciler | Depends on the response contract (B4) + AT confirmation on gap handling (B6). | Job that emits ABORT/void records for `failed` `fiscal_issue_attempts` gaps if AT requires. |
| **D9** | Revoke anon **sequence** grants | `generate_transaction_number` (SECURITY INVOKER) still uses `transaction_number_sequence` until the fiskaly cutover replaces it. | Revoke sequence grants when `pos-checkout` fully replaces the legacy numbering. |
| **D12→D14** | Tenant-scope sync RPCs + REVOKE FROM PUBLIC | Smoke test (2026-07-06) found sync RPCs anon-executable; the PWA design (§4.3) then found the deeper issue — several `SECURITY DEFINER` sync RPCs (incl. the **verified anon/cross-tenant write+delete primitive `upsert_transaction_with_items`**) bypass RLS and **don't self-enforce tenant**. Corrected the D12 wording: `REVOKE … FROM anon` is insufficient (genesis granted anon explicitly + PUBLIC default) — must `REVOKE … FROM PUBLIC, anon`. | ✅ MIGRATION WRITTEN + reviewed (`7a6a0a3`, `20260717000000`): all 9 RPCs tenant-scoped, REVOKE FROM PUBLIC,anon + GRANT authenticated, `upsert_transaction_with_items` device-only. ✅ **DEPLOYED to prod — CORRECTION 2026-07-27:** this row previously read "⛔ NOT deployed (B2/B3)"; that was stale. `20260717000000` **is in the prod remote ledger** (see D-MIG1). ⚠️ **Still owed:** the post-deploy verification the migration file itself asks for — the anon-key + cross-tenant probe checklist at the end of the file has **not been run**, so tenant-scoping is applied but not empirically proven on prod. **Deploy prereq (satisfied on prod):** callers' JWTs must carry `app_metadata.tenant_id` (paired tills do) or sync fails closed. Staging remains unpushed (B3). |
| **D13** | Connectivity pill accuracy (minor) | On the login screen the status pill briefly showed "Offline" though the in-app `ping` RPC returns `200`. Not a regression from D6 (connectivity is an independent 5s heartbeat). Likely a first-render/subscription-timing display quirk. | Low priority: have the pill subscribe to `connectionStatus` and force an initial `forceCheck()` on mount; re-verify. Part of D4 ConnectivityGate UX. |
| **D15** | Drop bare-global uniques before multi-tenant writes | `products_sku_key`, `transactions_transaction_number_key`, `employees_employee_number_key` (genesis, kept by phase1) are global-unique. With `upsert_products` now on `ON CONFLICT (tenant_id, sku)`, a new `(tenant_id, sku)` whose `sku` exists under a **different** tenant misses the composite arbiter and raises a raw `unique_violation` from the bare key. **Harmless today** (single default tenant), but must drop the bare uniques (keep the composite `(tenant_id, …)` ones) before tenant #2 / PWA writes. | 🧊 tracked — drop bare uniques in the multi-tenant write-path phase (P4/P7). Same class hits `transaction_number` inside `upsert_transaction_with_items` (surfaces as `success=false`). |
| **D16** | Tenant-scope cross-entity FKs (follow-up) | `products.category_id → categories(id)`, `transaction_items.category_id/product_id` validate against the **global** PK, not the caller's tenant — so a caller can reference another tenant's category/product id (no data is read back cross-tenant, but it's a latent integrity hole). | 🧊 follow-up: composite FKs `(tenant_id, category_id)` etc. once the per-tenant composite targets exist. Low severity. |
| **D17** | `pwa-login` brute-force rate-limiting | The `pwa-login` edge fn (deployed) is enumeration-resistant (one generic error) but has **no per-identifier/IP throttling** yet — a password can be brute-forced. | 🧊 follow-up before heavy use: add an attempts table (like the employee lockout) or edge rate-limit. Noted in the fn header. |
| **D18** | `transactions.store_id` / `daily_sales_summary.store_id` pulled forward for store-scope | Store-scope is now a SECURITY requirement (user decision), but sales/reports carry **no** `store_id` today — so store-scoped sales reads are impossible until it's added (+ device→store backfill + RLS `store_id = ANY(app.store_ids())`). | ⏳ P1/P2: add the columns + backfill + RLS store predicates with the read surfaces (where store-scope first bites). Tenant-level catalog stays shared. |
| **D19** | Test fixture: `testowner` human on prod | `provision-human.mjs` created `test-owner@easysoft.local` / username `testowner` (owner, default tenant) on EasySoft to verify auth. Known password. | 🧊 EasySoft is pre-production (C3, wiped at real provisioning); delete this user before production, or keep as a QA fixture. |
| **D20** | Settings/Employees fine-grained owner/admin gating (refactor Step 7) | — | ✅ DONE (`0f56c41` + fix in `3309f6d`): Settings `isSystemAdmin \|\| owner`; Employees keeps `isCurrentSystemAdmin` (ADMIN001 identity, owner-extended) SEPARATE from `canManageAdminEmployees` (admin/owner) — no escalation. A replace_all TDZ crash was found + fixed via browser verify. |
| **D-P2** | PWA read surfaces (reports/data) | — | ✅ DONE (`3309f6d`): host-aware PostgREST read source (`fetchAllPages`, paginated + id-chunked, `.is(deleted_at,null)`, throw-on-error). Verified in-browser: Products/Categories/Employees show real data; Reports queries live (€0.00 = tenant has 0 tx). Writes guarded (PWA view-only v1). **Still open:** the store dimension (D18) + wiring management writes. |
| **D-mobile** | Wide-table mobile reflow | Mobile nav is fixed (`6bbbeb9`: hamburger → drawer, verified 390px). No page overflow anywhere. But the Products (720px) + Customers (1280px) tables scroll horizontally within their cards rather than reflowing to mobile cards. | ✅ DONE (2026-07-08, mobile UI pass `2772be9`→`cc0c0b8`): full phone-viewport (PWA) redo, verified at 390px via Playwright + confirmed desktop pixel-identical at 1440px. Products/Customers desktop `<table>` is now `hidden md:block` with a `md:hidden` stacked card list (same rows + actions kebab); Dashboard/Transactions/Inventory/HR stat cards go 2-up on mobile (were full-width 1-up); Transactions rows + Dashboard/Employees headers stack vertically; Employees header button no longer clips off-screen. All changes gated behind `sm:`/`md:` so desktop + tablet are untouched. 🧊 Left as acceptable-not-broken: ProfitCosts/StockProfit/CashDrawerAudit icon-left KPI cards (readable 1-up), the English PWA install banner (i18n nit, not layout), a few raw `€{value}` formatters not yet using the country locale. |
| **D-vercel** | Deploy the PWA to HTTPS (Vercel) | PWA infra is built + installable (`e474fcf`: manifest + SW + icons, verified SW-controlling on localhost). It only becomes installable for real users once served over **HTTPS** — the SW/install don't work on plain HTTP. | ⏳ user: deploy the `pwa` branch to Vercel (`vercel.json` present; base is absolute on Vercel). Ties to B10/S13 (Vercel env). Then "Add to Home Screen" works on phones. |
| **D23** | Dexie data-at-rest in the browser | Partially addressed: the startup **seed** is now gated to the till (`e474fcf`) so no demo data is written to a manager's browser. | 🧊 Follow-up: fully disable/evict Dexie on `isPwaHost` (some paths may still open it) per plan §2.5. Low severity (no tenant data synced to browser). |
| **D30** | Delete the legacy fiscal-issuer edge fns (coordinated cutover) | `fiskaly-/vendus-/invoicexpress-fiscal` are hardened (T7) but still exist + use global provider creds. The client's `checkoutOrchestrator` (`src/fiscal/checkoutOrchestrator.ts:32-63`) still dispatches to them by `settings.fiscal.issuer`; deleting them while a till is configured on `fiskaly/vendus/invoicexpress` breaks that till's checkout. | 🧊 sequenced: (1) confirm/force every till onto `local_at`/`pos-checkout` and that none dispatches to the legacy issuers; (2) then delete the 3 functions + remove `FISKALY_/VENDUS_/INVOICEXPRESS_*` provider env vars. Do with the client `pos-checkout` cutover (C4/T6). |
| **D10** | fiskaly provisioning tool | Now unblocked by working creds (B1). Needs the create contracts for taxpayer/location/system (taxpayer `type` discriminator not echoed — get it from fiskaly's OpenAPI/Postman, or reverse-engineer). Must persist the **System id** for A5. | Build a `scripts/provision-fiskaly.mjs`: org(UNIT)→taxpayer→location(BRANCH)→system(FISCAL_DEVICE); write ids into `tenant_fiscal_config`. Then do a TEST issuance to close B4/A2–A5. |
| **D-rule** | Product rule: when a sale escalates FS/FR → FT (INVOICE) | Needed so the till knows when a validated NIF must appear (A7). | Decide: e.g. "NIF requested + amount over threshold, or customer asks for fatura" → INVOICE; else FS/FR with optional `customer.code`. Freeze before Phase 4 LIVE. |
| **D11** | **SIGN ES (Spain) fiscal integration** — future target market | Spain is also a target (user, 2026-07-06). The account **already has a working SIGN ES TEST key** (`azp: sign-es`, org `group1`) — so ES is testable now, but the user chose **"not now."** ⚠️ SIGN ES uses a **different fiscal model** (Veri*factu / TicketBAI-Basque), **not** the PT RECEIPT/INVOICE+ATCUD contract — so `pos-checkout` (PT-specific) does **not** carry over; Spain needs its own issuer path. | user | ⬆️ PROMOTED → **B15** (2026-07-08, fiskaly email: ES most urgent) | Tracking moved to B15. The per-country pluggable-issuer principle stands. |
| **D-ES1** | **Recargo de equivalencia NOT modeled on products** (ES) | Equivalence-regime retailers need a per-line surcharge (5.2/1.4/0.5%) alongside IVA; the one-rate-per-product model can't express it. SIGN ES supports it at issuance (`additional_vat`), so it's an input-model gap, not an issuer gap. | 🧊 LOUD DEFERRAL (2026-07-08): ES v1 targets non-equivalence merchants. Before onboarding an equivalence-regime retailer: add a surcharge field to products + line math + `additional_vat` in the ES issuer. Founder confirms whether the ES pipeline includes such retailers (brief §5). |
| **D-ES2** | **Operating-country setting (PT/ES) + full ES localization** — the big country switch | User asked (2026-07-08) for a Settings option to pick operating country that drives *"literally every potential difference"* between PT and ES, plus a Spanish UI. | ✅ **CORE DONE (2026-07-08)** — build + `tsc --noEmit` clean, **0 new test failures** (baseline 13 pre-existing test-infra fails unchanged). New `src/lib/countryProfile.ts` is the single source of truth (locale, EUR, VAT rates, allowed fiscal issuers + default, tax-id label/placeholder/**validator** with real PT mod-11 + ES DNI/NIE/CIF checksums, postal mask, phone prefix, default language, timezone). Wiring: `settings.operatingCountry` (`SettingsContext`, default PT, migrated on load, mirrored to `localStorage.operating_country` + module-global `setActiveCountry`). Settings gets a **country SegmentedControl** (admin) that on-change resets issuer→country default, IVA→standard rate, and UI+receipt language. **Fiscal issuer gating**: ES offers only `sign_es`; `fiscalIssuerLabel` learns SIGN ES; ES routes to a `renderSignEsSetup` card (not the Fiskaly-PT config); **SAF-T card hidden for ES** (closed the bug where an ES operator could emit a bogus PT SAF-T). **VAT** pickers (`ProductForm`, `CustomInvoiceDialog`, Settings default-IVA) now call `ivaRatesForCountry` + country default rate, and preserve an out-of-set stored rate so the select never blanks. **Identity**: NIF label/placeholder/inputMode + postal mask (PT `NNNN-NNN` / ES `NNNNN`) + default customer country now country-aware in `CustomerForm`/`CustomerDialog`/`CustomInvoiceDialog`; `fiscalCustomer.buildFiscalCustomerFields` no longer leaks `999999990`/"Consumidor Final"/"Portugal" onto ES docs (ES omits the buyer tax id). **Formatting**: fixed *Spain-gets-en-US* money/dates (`Reports`/`Dashboard`/`ProfitCosts`/`Employees` gained an `es-ES` branch), deterministic country locale on the live cart total + `Transactions`/`DeliveryOrders` (+ fixed a pre-existing `en-US` date bug there). **i18n**: full **`es` block — 2113 keys** (translated by 10 parallel agents, overlaid onto the `en` structure so keys can't drift; 0 placeholder mismatches), 3-way language cycle (en→pt→es), `supportedLngs`+region-fold, receipt language `'es'` (+`es-ES` receipt date locale, `'Efectivo'` cash detection), UI language defaults to the country (still user-overridable). 🧊 **LOUDLY DEFERRED (not done — polish/edge, none block PT or a fresh ES shop):** (1) **product-catalog rate MIGRATION on country flip** — the picker degrades gracefully (shows the stored rate) but `signEsFiscalIssuer` forwards the stored rate verbatim, so a PT-rated product (0.23) issued under ES is **rejected by fiskaly**; needs a catalog-only remap (23→21, 13→10, 6→4\|10) that never touches hash-signed history — business decision. (2) **phone**: `DiscountDialog.normalizePhone` strips only `+351` (breaks ES loyalty phone lookup); `phonePlaceholder` i18n still `+351`. (3) **provincia field** not added (ES COMPLETE invoices; `signEs` still sends `postal_code:'-'`). (4) **ES doc title**: `saleDocumentType` FS→FATURA, so an ES simplified ticket prints "Factura" not "Factura simplificada". (5) **timezone** (`Europe/Madrid`) day-boundary not wired — Z-report/day buckets use the device clock. (6) residual hardcoded locales: `Header` clock + `ReceiptHistorySelector` (`pt-PT`), HR payslip (`en-GB` + `<html lang="en">`), `Devices.tsx` untranslated strings. (7) in-app **SIGN ES onboarding FORM** (backend `provision-sign-es` ready). |
| **D-N1** | **Notifications P3a** — in-app alert feed (server producers + PWA feed) | Scope chosen by user: *in-app feed + background push*, *server-detected + till heartbeat*. Plan: `docs/pwa-notifications-plan.md`. | ✅ DONE. **Server** (`1713b0e`, deployed to EasySoft via `db push`): 5 migrations — producer helpers + de-dup index + default `alerts` config, trigger producers (REFUND/FISCAL_CANCELLATION/LARGE_DISCOUNT on `transactions`, FISCAL_ISSUE_FAILED on `fiscal_issue_attempts`, DRAWER_OPEN_NO_SALE dormant), `notification_read_state` + RLS, append-only hardening (CI-asserts RLS+policy), Realtime publication. Producer verified firing on prod (prior session). **Client** (`326de12`, typechecked + `vite build` clean): `notificationTypes.ts`, `NotificationFeedContext` (one RLS-scoped Realtime channel/tenant, backfill on every SUBSCRIBED, visibilitychange catch-up, optimistic read cursor), `setAuth` lifecycle in `SupabaseAuthContext` (sign-in/refresh/restore + fail-closed clear on sign-out, PWA-gated), `NotificationBell`+`NotificationPanel`, PWA-only `Alerts` Settings tab (reads/writes `tenant_settings.data.alerts`). ⚠️ **Live Realtime *delivery* not empirically re-verified this session:** headless proof needs a privileged `notification_events` insert (authenticated lacks the table grant; producers emit via SECURITY DEFINER), and the DB-owner injection was **blocked by the prod-write guardrail** with no non-prod target (no local Docker; staging pw unknown, B3). What *was* verified this session: testowner sign-in carries `app_metadata.tenant_id`, and `supabase.channel('notif:<tenant>').subscribe()` reaches **SUBSCRIBED** with the real user JWT. Delivery relies on the standard Supabase `postgres_changes` + RLS(`app.tenant_id()`) path — the same claim helper that governs the PWA's already-working PostgREST reads. **Acceptance test:** on the deployed PWA, do a real till action (large discount or refund on a synced sale) and watch the bell update live. |
| **D-N2** | **Notifications P3b** — Web Push (background delivery) | Second half of the user's chosen scope. Push fans out via a `tenant_members` INNER JOIN (revocation) and the delivery trigger is EXCEPTION-guarded so it can never roll back the till's fiscal transaction. | ✅ CODE DONE + DEPLOYED (`24bf510`, migrations pushed + `notify-push` fn deployed): `push_subscriptions` (composite unique) + RLS, SW `push`/`notificationclick`/`pushsubscriptionchange` + IndexedDB kv, `src/lib/push.ts` (opt-in/disable/reconcile, shared-device re-own), `notify-push` (membership join + endpoint dedupe + 410 prune + `delivered_at`), delivery trigger (exception-isolated) + null-safe `pg_cron` backstop (`6c8af0f`), Settings opt-in toggle, auth-context reconciliation. ⏳ **ACTIVATION IS MANUAL (classifier-gated):** edge secrets (VAPID + shared secret), Vault seeds, Vercel `VITE_VAPID_PUBLIC_KEY`, push→main — all in **`docs/pwa-notifications-activation.md`**. Push DID nothing until the Vault/edge secrets were seeded — **now activated** (edge secrets set, Vault seeded, `notify-push` smoke-tested: wrong-secret→401, correct→200). SIGNED_OUT now unsubscribes this device's push (review fix, see D-N5). Remaining device-side: install PWA → toggle on → real critical event to see the buzz. |
| **D-N3** | **Notifications P3c** — till heartbeat + offline alert | The `alerts.offline` toggle ships in the PWA Settings tab; now it actually fires. | ✅ DONE + DEPLOYED (`24bf510`): presence enum/columns + `DEVICE_OFFLINE`/`DEVICE_ONLINE`, `device_heartbeat` RPC (device-role gated, self-scoped, ONLINE on recovery), `sweep_device_presence` + `pg_cron` (live now, ~grace+60s latency), `src/services/deviceHeartbeat.ts` (Electron/device-session only, 60s), `Devices.tsx` presence pill + `manage-devices` returns `presence`. |
| **D-N4** | **Notifications P3d** — human revocation propagation (before GA) | A removed manager keeps feed + push until their JWT expires (≤1h). Feed RLS re-checks per-row; the concern is the in-flight token + off-device push. | ✅ CODE DONE + DEPLOYED (`24bf510`; hook fix `95f3065`): `revoke-human` fn (membership + push-sub delete + active-claim re-stamp) deployed; `custom_access_token` hook fn deployed (SECURITY DEFINER, **skips device sessions** so it can't brick till login). Membership delete now goes through the last-owner-guarded `revoke_tenant_member` RPC (D-N5). ⏳ **REMAINING:** (1) ENABLE the hook in Dashboard → Auth → Hooks (see activation doc); (2) `admin.signOut` needs a JWT not a user-id → no immediate forced-logout; the hook bounds feed/REST leak to ≤`jwt_expiry`; push closed immediately by sub-delete + delivery join. (3) No in-app team-admin UI removes members yet — `humanManagementService.revokeHuman()` is the ready client entry. |
| **D-N6** | **Credit-note sync silently lost fiscal transactions** (found during E2E credit-note verification, 2026-07-08) | Driving the till E2E (order → FT receipt → NC credit note) produced NO notification. Root cause chain, probed via a temporary PG_EXCEPTION_CONTEXT debug twin: (1) `upsert_transaction_with_items` declares OUT column `transaction_id` and its item-replace ran `DELETE FROM transaction_items WHERE transaction_id = txn_id` → plpgsql ambiguity error. Sales never hit it (they exist server-side at checkout → S17 sealed guard returns early), so ONLY locally-created-only transactions (credit notes) died. (2) The RPC returns per-row `success:false` over HTTP 200 and the client trusted the transport → failed rows marked synced → never retried. **8 of 11 local fiscal transactions (FT0001–0007 + NC0010) were missing from the server.** | ✅ FIXED + DEPLOYED + RECOVERED (`024c869`, reviewed pre-deploy by an adversarial workflow that caught a HIGH bug in the new fd-trigger — phantom `gross_total` column that would have aborted pos-checkout's evidence INSERT — plus missing EXCEPTION guard + missing live/training filter; all fixed before deploy): migration `20260726002000` (alias-qualified DELETE, byte-identical otherwise; drops debug helpers; hardened `fiscal_documents` NC producer for the future fiskaly/pos-checkout path), client per-row success check. Recovery: flipped `needs_push` on the 8 lost rows → till's own sync pushed **all 11** documents; `CREDIT_NOTE_ISSUED` fired + `delivered_at` stamped (push delivered). Also fixed en route: Electron `window.prompt` crash in the credit-note flow → in-app web modal + `forceSync` (`1d2ad81`). 🧊 Review follow-ups (pre-existing, low): `lastPushedAt`/pending-ops counter advance even when all rows fail (sync pill can read healthy while rows are stuck — ties to D13); permanently-failing rows retry every 5min forever with no operator surfacing. |
| **D-N5** | **P3 adversarial security review** (ultracode workflow, 2026-07-07) | 5-dimension review (isolation, auth-hook, push-revocation, producers/heartbeat, client) → each finding independently verified by a skeptic. **Tenant-isolation and the auth-token hook came back CLEAN.** 7 findings confirmed, 1 refuted. | ✅ ALL 7 FIXED + DEPLOYED (`8c122a3`, migration `20260725000000` + `notify-push`/`revoke-human` redeployed + smoke-tested): (HIGH) `notify-push` no longer stamps `delivered_at` when the subs/members READ errored → was a silent permanent drop of a critical fiscal alert; (MED) same for transient send failures (429/5xx/timeout) — leave NULL so the cron retries, bounded to 24h; (MED) `sweep` floors `grace_seconds` at 120s (0/blank flapped the whole fleet) + re-asserts staleness inside the UPDATE (kills concurrent-heartbeat spurious OFFLINE); (MED) `revoke_tenant_member` RPC locks owner rows FOR UPDATE + refuses last-owner removal; (MED) SIGNED_OUT now unsubscribes push (shared-device leak); (LOW) unread cursor uses server-stamped `created_at` not client clock; (LOW) grace input no longer coerces blank→0. Refuted: the vault-read exception "nit" (unreachable on Supabase + the sync RPC's own EXCEPTION block already catches it). |
| **D-AI1** | **Assistant KB content is PT-authored** (from the newsoft feature integration, 2026-07-11) | `ASSISTANT_QA.md` / `TUTORIALS.md` assert Portugal-specific tax/legal facts as universal (IVA 23/13/6 vs ES 21/10/4; "In Portugal you can't delete a fiscal sale"; certification claims). ES tenants exist since D-ES2. **Prompt-level mitigation shipped** (branch `assistant-whatsapp`): system prompt now carries `tenants.country`, instructs Spain-only tax/legal answers + flags PT-only snippets, and allows Spanish replies. The CONTENT itself is still PT-only. | 🧊 LOUD DEFERRAL: author ES variants of the compliance QA answers (+ ideally a `country` column on `kb_documents` with per-country retrieval), re-run `npm run build:kb`. Until then ES tenants rely on the prompt guardrail. |
| **D-AI2** | **Assistant/WhatsApp rate-limiting + cost caps** | Upstream's own header notes: `whatsapp-webhook` has NO per-sender throttling (pairing brute force is attempt-capped at 8, but message flood → unbounded Soniox+Anthropic spend); `assistant`/`transcribe` fns have no per-user rate limit either. Same class as D17 (`pwa-login`). | 🧊 before heavy use / public rollout: per-number and per-user throttle (attempts table or edge rate-limit) on `whatsapp-webhook`, `assistant`, `transcribe`. |
| **D-AI4** | **Assistant "today" anchored to Europe/Lisbon for ES tenants** | `lisbonToday()` in `assistant/index.ts` + `whatsapp-webhook/index.ts` tells the model "today" in Lisbon time, while the sales RPCs bucket by till-stamped `transaction_date` (Madrid-local for ES tills — D-ES2 item 5). Between 00:00–01:00 Madrid an ES owner asking "today's sales" gets the previous Madrid-day. Same class as the already-deferred country timezone wiring. | 🧊 fold into D-ES2 (5): derive the prompt timezone from `tenants.country` (Europe/Madrid for ES) when the day-boundary work lands. ~1h/day exposure window, reads only. |
| **D-K1** | **Kiosk lockdown is deliberately incomplete** (2026-07-12) | Packaged tills now run kiosk-fullscreen with the application menu removed (no Alt reveal). BUT, per user decision: **DevTools stays reachable (F12 / Ctrl+Shift+I)** for field debugging, and **Alt+F4 still quits** (needed as the only exit — there are no window buttons). A determined operator can open DevTools and inspect/tamper. | 🧊 LOUD DEFERRAL: before tills ship to real stores — remove the `before-input-event` DevTools binding in `electron/main.js` (marked ⚠️ in-code), decide the sanctioned exit path (admin-PIN quit? `Ctrl+Alt+Q` accelerator?), and consider blocking Alt+F4. |
| **D-K2** | **No re-pair path when pairing scope exists but the device session is dead** (2026-07-12) | The unpaired-till redirect (`/login` → `/pair-device`) keys off `hasDevicePairingScope()` (localStorage). If scope EXISTS but the Supabase device session is revoked/expired/lost, login fails with "This till is not paired" and the kiosk had **no in-UI way back to /pair-device** (no URL bar). | ✅ RESOLVED (2026-07-12): the "till is not paired" login failure (sentinel `TILL_NOT_PAIRED_ERROR`) now renders a **"Re-pair this till"** button → `/pair-device`; the pairing page shows **"Back to sign in"** when scope exists, so a mis-tap isn't a dead end. Successful pairing overwrites the stale scope. |
| **D-SC1** | **Sold-by-weight sale flow (Phase B)** (2026-07-13) | Phase A shipped: CAS PR-II driver in the Electron main process (`electron/hardware/scaleController.js`, probe auto-detect, live readings over IPC), Settings → Hardware → Scale diagnostics panel, mock mode (`SCALE_MOCK=1`). **A weighed product still cannot be sold.** Phase B is ⛔ **hard-gated by a DB migration**: `transaction_items.quantity` is INTEGER and `upsert_transaction_with_items` casts `::INTEGER` — a fractional quantity from the till would make the RPC raise, the swallow-all handler returns `success=false`, and the sale **strands in the sync outbox forever**. Also required before Phase B ships: fiskaly SIGN ES serializes quantity at 2dp (`signEsFiscalIssuer.ts:63,150` — needs an empirical fractional-quantity TEST sale), SAF-T emits Quantity 2dp + hardcoded `UnitOfMeasure 'UN'` (`exportSaft.ts:282-284`), ThermalReceipt prints a hardcoded per-line 'Unidade' label (`ThermalReceipt.tsx:467`), cart +/- steppers and same-product line-merging assume integer quantities (`POSContext.tsx:77-85`), `products` has no `sold_by_weight` column, and `daily_sales_summary.items_sold` is INTEGER (decide: weight lines count as 1 item or as kg?). | ✅ CODE COMPLETE (2026-07-13), ⏳ DEPLOY-GATED: full Phase B implemented per user decisions (3dp end-to-end; kg stock; items_sold sums kg; separate line per weighing; **manual entry NOT built** — v1 scale-only, user confirming the PIN-gated design first). Adversarially reviewed (24 agents): 17 findings confirmed + fixed — notably the needs_push outbox path dropping `unit` (offline weighed sales + ALL credit notes would have landed server-side as 'un' and clobbered the till's own rows on next pull) and per-line cent quantization in `saleCheckoutDraft` (charged = printed = synced = AEAT-registered totals identical by construction; the old unrounded floats could drift a cent and the ±0.02 SIGN ES guard could block large weighed carts). Build/tsc/tests verified at the pre-existing baseline. **REMAINING: (1) ✅ PUSHED to EasySoft 2026-07-15 (user-initiated `db push`; applied in one transaction). Post-deploy checklist verified via API: anon DENIED (42501) on both recreated sync RPCs + assistant RPCs stay service_role-only; authed `get_products_delta` returns `sold_by_weight` + numeric stock; `get_transaction_items_delta` returns `unit` ('un' backfilled) + numeric quantity. Device-JWT fractional upsert = covered by the first weighed sale on the test till. Staging still unpushed (B3 — password unknown). (2) fiskaly SIGN ES fractional-quantity TEST sale before first LIVE weighed sale; (3) real-hardware till test (D-SC2); (4) manual-entry decision (D-SC3).** Note: `tests/fiscal/*` fail at import PRE-EXISTING, so the SAF-T KG/3dp change has no runnable test — verify the first SAF-T export containing a weighed sale. FOLLOW-UP 2 (2026-07-15): weigh-flow UX redesigned after user testing — the dialog used to run detection per tap and flap between 'Waiting for scale…' and 'Scale unavailable' as the controller's scan/5s-retry loop cycled. Now the scale session is **till-scoped** (started at POS mount, never stopped by dialogs; Settings panel only stops a session it itself started), the dialog is a pure consumer (instant when scale present), the offline state is ONE stable panel ('Scale not detected — check power/cable. Retrying automatically…' + Try again + manual entry), and a **scale status chip** lives in the POS bottom bar (green/blue/red/gray). FOLLOW-UP FIXED (2026-07-14): the sold-by-weight toggle was initially placed inside ProductForm's inventory block, which is disabled behind `{false && (…)}` ('preserved for re-enable') — it never rendered; moved to the visible Pricing & Tax section (price label shows €/kg). ⚠️ Related gap, pre-existing: while that inventory block stays disabled, product stock (kg or units) has NO UI entry point in the product editor — decrement-on-sale works, but initial stock is set only via seed/legacy paths. Decide whether to re-enable the block when weighed products need real kg stock entry. |
| **D-SC2** | **serialport has never run inside a packaged Windows build** (2026-07-13) | The module ships (deps + win32-x64 prebuilds verified unpacked to `app.asar.unpacked` by a local `--dir` build, same smartUnpack path as the working `usb` module) but no packaged Windows exe has ever `require`d it. The scale controller loads it lazily in try/catch, so a binding failure degrades to a diagnosable error instead of a startup crash. | ⏳ First CI Windows artifact after this change: on the till, open Settings → Hardware → Scale → "Detect scale" and confirm ports list + detection. If `require` ever fails inside asar, add `asarUnpack: ["**/node_modules/@serialport/**"]` to package.json build config. |
| **D-SC3** | **Scale readiness-gate classification undecided** (update-policy §7.2 / open decision O1) | The fail-closed boot gate is designed-not-built; every hardware precondition needs a class (Blocking / Tender-specific / Degraded / Informational). Proposed for the scale: analogous to the cash drawer's tender-specific class — **scale unavailable blocks only weighed-product sales** (manual weight entry as the sanctioned fallback, or blocked entirely — user/compliance call). | ✅ RESOLVED (2026-07-15): user confirmed — **manual weight entry BUILT**. Scale-offline impact stays weighed-products-only (never blocks the till). The weigh dialog's offline panel now carries the manual entry: weight field + **manager/admin employee number + PIN** (verified server-side via the same `employee_pin_login` RPC as the login screen, so failed attempts hit its lockout) + **audit log** (local Dexie `manualWeightAudits`, v16: product, weight, cashier, authorizer, scale state, terminal). ⚠️ Two caveats: (1) authorization requires the server (consistent with T1 online-required v1); (2) the audit table is **local-only** — no server sync yet; add a server table + push if fleet-level visibility is wanted. Readiness-gate class for the future boot gate: tender-analog (weighed products degraded to manual entry). |
| **D-AI3** | **Assistant migrations renumbered — remote-history caveat** | The 5 feature migrations arrived as `20260720000000..20260721000001`; `20260721000000` collided with `notif_producer_helpers` and all sorted before the already-applied local head, so they were renumbered to `20260729000000..04` (`607818b`). ⚠️ IF the newsoft fork was ever `db push`ed against the SAME Supabase project under the OLD versions, the renamed files would re-apply (they are idempotent-ish: CREATE OR REPLACE / IF NOT EXISTS, but policies use DROP+CREATE). Could not verify remote history from here (prod-read guardrail). | Before `db push`: `supabase migration list --linked` — if `20260720*`/`20260721000000` (whatsapp) versions appear as applied, `supabase migration repair` them onto the new versions instead of re-applying. |
| **D-U1** | **Update-policy Stages 0–3 implemented; O1 gate classification is an INTERIM DEFAULT** (2026-07-24, branch `update-stages`) | Runtime config (`userData/config.json`: environment/supabase pair/`ui_origin`/`renderer_source`), fail-closed boot gate (`app://gate/`, checks + proceed re-validated in main), network repoint with origin allowlist, min-shell handshake (`shell-requirements.json` + in-UI block; `HARDWARE_API_VERSION=1`). Full map: `docs/update-policy.md` §15; ops: `docs/till-update-runbook.md`. ⚠️ Gate classification defaults chosen WITHOUT O1 sign-off: **printer + drawer = degraded (warn-only, never block boot)**; blocking = config validity, internet, backend (only when config.json carries `supabase_url`), UI source, shell handshake. NOT yet shell-checkable: device session, fiskaly, checkout-fn health (renderer-side guards remain the mid-shift net). | ⏳ user/compliance: ratify or flip the printer/drawer classes (is a dead receipt printer legally blocking in PT/ES?); install-time `config.json` provisioning so the backend check activates fleet-wide; future: session/fiskaly/checkout preflight items. |
| **D-U2** | **electron-updater remains deferred after Stages 0–3** (plan D10 / update-policy O2·U2) | Stages 0–3 make UI updates install-free, but a native/shell fix still needs a manual reinstall per till. An UNSIGNED auto-update channel (code-signing DECIDED unsigned for v1) is a real security trade-off — not slipped in silently with this work. | ✅ RESOLVED (2026-07-24): **user explicitly accepted the unsigned channel** ("we want the auto-update now! even tho unsigned (for now)") → implemented as D-U5. Feed-host choice still open there. |
| **D-U3** | **Tenant subscription plans: provisional names + prod linkage fix pending** (2026-07-24) | `20260801000000` adds `tenants.subscription_plan` (CHECK `trial|basic|standard|premium` — names INVENTED, no product tiers defined anywhere) + `app.validate_tenant_member_store_ids` trigger + `provision-human` store_ids existence validation (uuid-shape → 400 `store_ids_not_uuids`, unknown → 400 `unknown_store_ids`). The testowner→store→tills prod fix is a runbook, not yet executed: `docs/tenant-onboarding-runbook.md` (gaps T-1..T-5 there, incl. the still-missing `admin-provision-tenant` pipeline and JWT re-stamp after SQL scope edits). | ⏳ user: confirm plan names; `db push` the migration (staging first — B3 password still unknown); run runbook §C on prod when tenants go real. |
| **D-U4** | **renderer_source flip = web-origin change: per-origin storage (pairing, Dexie outbox, session) does not follow** (adversarial review 2026-07-24, HIGH) | Flipping bundled↔network moves the UI between `app://pos` and `https://<ui_origin>`; localStorage/IndexedDB are origin-scoped. | ✅ LARGELY FIXED (2026-07-24, same day — user ordered the §6.2 pull-forward): shell **device store** (`electron/deviceStore.js`, safeStorage `device-store.safe`) now owns pairing scope + Supabase session; sync preload snapshot → `src/lib/shellDeviceStore.ts` hydrates localStorage BEFORE the Supabase client constructs (first import of main.tsx); write-through via custom auth storage adapter + `saveDevicePairingScope`; first boot on a store-capable shell adopts existing localStorage identity (paired tills keep pairing on shell upgrade); old shells/browser hosts no-op (P1). Post-review hardening (same day, 26-agent pass): main-side key allowlist + key/value/store size caps (remote-UI abuse → durable-DoS closed), atomic tmp+rename writes, fail-LOUD on resolved {success:false} mirrors, freshness-aware hydrate (fresher local session repairs the shell instead of being clobbered), adoption gated on never-written store (no resurrection of signed-out identity from a stale origin), single-active-project purge of foreign sb-* tokens. **Still per-origin: Dexie** — an unsynced outbox stays parked under the old origin, so runbook §B keeps the empty-outbox + day-closed pre-flip checklist (re-pair/re-login no longer needed). Gate ES localization still deferred here (strings PT/EN; fold into D-ES2 residuals). |
| **D-P1** | **Platform (sysadmin) console — cross-tenant control plane** (2026-07-24, user request; pulls forward the "back-office" from plan §6.5 and the UI slice of runbook gap T-2) | Built per plan §6.5's rule (service role from back-office only, **zero RLS carve-outs**): `20260802000000` adds `platform_admins` (service-role-only SSOT) + append-only `platform_audit_log` + access-token-hook v2 (also strips a stale `platform_admin` claim) + `platform_delete_tenant` (guarded: refuses any tenant with transactions/fiscal documents; multi-pass FK-ordered teardown in ONE transaction; device auth users cleaned by the fn). New `platform-admin` edge fn (caller checked against the TABLE per request, never the claim; every mutation audited): tenants list+counts/create/edit(status, plan)/delete, stores, members (create owner with platform authority / remove via the last-owner-guarded RPC + same off-device cleanup as revoke-human), ALL tills (create+pairing code, reissue, rename, revoke — same hash/TTL/single-use semantics), audit view. `pwa-login` falls back to platform login when a user has NO memberships (disjoint by design). Client: `source:'platform'` principal (role `sysadmin`), `/platform` console page (PlatformRoute), Sidebar entry; platform principals are locked out of every tenant permission. Bootstrap: `scripts/provision-platform-admin.mjs`. Runbook: **`docs/platform-console-runbook.md`**. **Adversarially reviewed pre-commit (24 agents, 18 confirmed / 1 refuted) — all 18 fixed**, notably: (HIGH, pre-existing class) **GoTrue MERGES app_metadata — spread-and-delete claim clearing was a NO-OP**, so membership revocation never actually revoked edge-fn authority (getUser reads the DB record); fixed with explicit-null stamps in platform-admin AND the pre-existing `revoke-human`, plus `store_ids: null` on every re-stamp site (pwa-login/switch-tenant — stale store scopes survived tenant switches); role/scope EDITS now restamp the live claim (create_member) AND the hook re-syncs app_role/store_ids from the tenant_members row on every token issue (demotions bind ≤1h even without re-login); hook enforces member↔platform disjointness at mint + tenant_id uuid cast made non-raising (a malformed metadata write would have blocked ALL token issuance for that user); tenant-delete TOCTOU closed (transactions/transaction_items/fiscal_documents excluded from the sweep — a mid-delete sale now aborts the whole teardown via FK instead of being destroyed); delete_tenant auth-side cleanup failures now ban-fallback + surface as residue instead of silent orphan credentials; audit-log FK dropped (bare uuid, notification_events convention); console detail-fetch generation guard (stale response could have caused wrong-tenant mutations); shared loading boolean → in-flight counter; empty `store_ids: []` normalized to null everywhere (would read as scoped-to-zero-stores). | ✅ DEPLOYED 2026-07-24 (user-run, verified): migration `20260802000000` in the remote ledger; all 6 functions deployed (platform-admin, pwa-login, manage-devices, provision-human, revoke-human, switch-tenant); web via Vercel Git push; first sysadmin bootstrapped (youneseslm@gmail.com / username `khalil`, user_id `175374ee…`) — starter password exchanged in-session, ⚠️ ROTATE it. Acceptance verified headlessly: no-JWT 401, anon-bearer refused, platform login → `platform_admin:true` session → `list_tenants` returned Default Tenant with live counts (7 devices, 4 online). **INCIDENT same day (user-hit): `create_member` failed "unrecognized JWT kid <nil> for algorithm ES256"** — the NEWLY-created `platform-admin` function's injected `SUPABASE_SERVICE_ROLE_KEY` is a new-format credential GoTrue's ADMIN endpoints cannot verify (PostgREST accepts it; older/redeployed fns like pwa-login got a working key — testowner login unaffected, empirically confirmed). Diagnosed by probing the same isolate: `list_members` emails came back null (admin READS failing silently) while PostgREST actions worked. Fix: all 7 admin-calling fns now prefer explicit secret `ADMIN_SERVICE_KEY` (legacy HS256 service_role JWT, verified 200 at GoTrue admin) over the injected env — ✅ RESOLVED same day: secret set + all 7 redeployed (user-run), re-verified headlessly (list_members emails resolve; create_member 201; probe member+auth user cleaned up). ⚠️ RULE for every FUTURE edge fn calling `auth.admin.*`: use the `ADMIN_SERVICE_KEY ?? SUPABASE_SERVICE_ROLE_KEY` pattern, never the injected env alone. ⚠️ Loud residuals (runbook §G): pwa-login rate-limit still missing (D17/D22) and now fronts platform login; training-mode toggle deferred; audit write is post-hoc (no rollback); tenant delete skips Storage objects; console EN-only. |
| **D-W1** | **Windows printer support was NEVER implemented — built 2026-07-25 (ships 0.1.2)** (user hit "Scan USB finds 0 printers" + ghost queues shown green on the real till) | Diagnosis (3-reader survey): every printer path was CUPS-shaped — "Scan USB" shelled to `lpinfo` (nonexistent on Windows → swallowed → always 0), the System tab ran `Get-Printer` with hardcoded `connected:true` (every ghost queue green, HPRT indistinguishable), "Use This" was cosmetic (selection never reached the print engine, both platforms), and the receipt/drawer/test print fallback used CUPS `lp`. Built: win32 USB scan via libusb cached descriptors (never hides driver-bound devices; interface-claim probe decides 'winusb' vs 'windows-driver' honestly), `usbwin://` direct-connect path with honest System-tab guidance on refusal, honest queue list via `Get-Printer` + `WorkOffline` (+UTF-8-forced stdout so PT accented queue names survive; shared by quick+full list), winspool RAW transport (`windowsRawPrint.js`, PowerShell Add-Type, Unicode W entry points, per-stage Win32 error codes) for receipts/drawer/test prints, `checkSystemPrinter` Get-Printer branch (load-bearing: sets isInitialized so sales can print), Windows "Use This" wires `setPrinterRole('receipt')` (macOS System-tab behavior deliberately unchanged; a held direct-USB device is released so the selection binds). **Adversarially reviewed pre-ship (16 agents): 10 confirmed findings fixed** — the critical one: test print bypassed the `isInitialized` gate that blocked real sales, so the operator would have seen a working test and a till that couldn't print receipts. | ⏳ ACCEPTANCE on the till: till-verified so far — 0.1.3 USB scan finds the HPRT (0.1.2's escpos-usb `findPrinter` silently dropped devices whose configDescriptor read failed → bypassed with direct `getDeviceList` + VID-based listing + diagnostics); user installed the official TP80K driver, queue appeared, Use This connected. First real winspool execution (2026-07-25) failed BEFORE PowerShell started: "The command line is too long" — `child_process.exec` routes through cmd.exe (8,191-char cap) and the -EncodedCommand payload (embedded C# helper → UTF-16LE → base64) is ~10K chars. Fixed in 0.1.5: shared `runPowerShell()` (execFile — direct CreateProcess, 32,767-char limit, no cmd) now carries ALL PowerShell call sites — **test print VERIFIED on the till (2026-07-25)**. Till then surfaced 3 more: (a) drawer test crashed "reading 'transfer'" — `initializeUSBPrinter` assigned `this.device` BEFORE `open()` (always refused for driver-bound printers) and dispatch keyed on bare handle truthiness; `printReceipt` had the IDENTICAL hazard, and `cashDrawerAuditService` runs `initialize()` (with full subnet scan) before EVERY drawer command incl. real cash sales; (b) Printers page perceived hang — unbounded fire-and-forget `quickListPrinters` stacking powershell.exe per remount; (c) selected printer not shared across the app. Fixed in 0.1.6 (user decision: printer config = app-wide SSOT): `printerTransport` drives dispatch (assign-after-open kills the poison), `initialize()` short-circuits to a single Get-Printer check when a queue is configured, roles persist to userData/printer-config.json (survives restart; fail-closed re-validation), new `hardware:get-printer-config` IPC + change event + `usePrinterConfig` hook (Electron test card shows the configured printer, no re-scan), quick-list coalescing, win32 Check-Status honesty (WorkOffline, not CUPS probes), win32 `getConfiguredPrinters`. 0.1.6 till-verified: printer AND drawer work — but drawer took 3-5s (one-shot PowerShell spawn + Add-Type C# compile PER JOB). 0.1.7: persistent raw-print worker (compile once at warm-up — boot/selection/init — then jobs over stdin in ~tens of ms; one-shot kept as fallback; identity-scoped timers + delivered-gate so an ambiguous no-reply job is NEVER auto-resent — "Print outcome unknown" instead of a possible double receipt/drawer kick; 14-scenario protocol harness green vs fake worker). 0.1.7 till-verified: drawer opens instantly from the Settings test card. The POS-page drawer (dialog AND every cash sale) did NOT open, and the operator could not type the required justification. **Two independent defects, both confirmed by a 3-lens investigation:** (1) READINESS GATES — `cashDrawerAuditService.issueOpenCommand` ran a full `initialize()` before EVERY open and returned early on failure, so the command was never sent; the test card worked precisely because it initializes once on mount and thereafter only sends. Behind it sat a SECOND gate (`electronHardwareService`'s own `isInitialized` early-return, which refuses before any IPC and made the first attempted fix dead code on a cold boot), and a THIRD (main's bare `isInitialized` refusal, false by design after restore). A failed `initialize()` also flipped the shared renderer singleton false, breaking the Settings card for the rest of the session. Receipts escaped only because `printerWorkflowService` prints via `testPrinterByName`, which bypasses readiness entirely. (2) TOUCH INPUT — the justification textarea had no on-screen keyboard (the app's `VirtualKeyboard` is used by CustomerDialog/CategoryForm/DevicePairing but not here), so on a keyboard-less till the field could never be filled and the button stayed disabled — and in every applied dialog style that disabled CTA renders as a solid filled button at 50% opacity, i.e. it looks tappable (proved by the repo's own dialog-audit screenshots). 0.1.8 fixes: send-first with initialize demoted to recovery; renderer readiness gates removed (main process is the single authority); `ensureReady()` accepts a persisted queue WITHOUT a Get-Printer probe (the winspool write is the existence test and returns staged Win32 errors — probing put a cold 15s PowerShell spawn in front of a physical action); `checkSystemPrinter` switched from wildcard `-Name` to exact match (a queue named with `[ ] * ?` prints fine but failed the probe); VirtualKeyboard wired to the justification field; disabled CTA flattened to grey + a hint line. Reachability of the fixed drawer path was traced gate-by-gate pre-ship (12 gates, cold-boot scenario) after the FIRST attempt shipped as dead code. Remaining acceptance on 0.1.8: type via the on-screen keyboard → drawer opens from the POS dialog → drawer kicks on a REAL CASH SALE. ⚠️ **Scope correction — this fixes the DRAWER only.** A prior register line claiming "first receipt after restart works without visiting Settings" was FALSE and has been removed: `electronHardwareService.printReceipt` has **zero production callers**, and the station path (`printerWorkflowService` → `testPrinterByName`) is reached only from `useOrderPrinting`, which has no consumers. **Sale receipts currently print through `ReceiptDialog` → `window.print()` (OS print path), NOT the winspool ESC/POS transport built in D-W1** — so the thermal path is today exercised only by the setup dialog's test print. ⚠️ Decide whether sale receipts should move onto the raw thermal transport (formatting, cut, no OS print dialog) — until then "REAL SALE prints" proves nothing about the winspool path. ⚠️ Residuals: other touch-input fields across the app have NOT been audited for the same missing-keyboard gap (the app has a `VirtualKeyboard` used by only 3 dialogs); a till whose transport persisted as `direct-usb` still routes a failed `ensureReady` into the renderer retry → `initialize()` → auto-discovery, which can claim a LAN device (strictly less exposed than before, when EVERY open re-initialized); `initialize()` still doubles as the UI's "is the printer ok?" verdict, and its false answer no longer implies the drawer path is dead; once `ensureReady` marks ready, a genuinely missing queue surfaces as the honest winspool "Win32 error 1801" rather than the "reselect it in Settings" hint. XP-90/XP-80C ghost queues still show connected (Windows never flipped WorkOffline; PnP cross-reference would be the deeper fix); hardware-change live rescan remains a disabled stub (pre-existing); `quickListPrintersWithStatus(true)` connectivity probes still macOS-flavored (list itself now honest). |
| **D-P2** | **Owner till-pairing from the PWA** (2026-07-24, same request) | `/devices` unlocked for membership owners/admins: the human JWT is the authority (manage-devices accepted it all along — the page just never offered the path); PIN unlock + Lock button remain till-only; membership path auto-loads. `deviceManagementService` credentials now optional. **Server hardening:** `manage-devices` enforces `store_ids` scope for store-scoped humans (list filtered; create/reissue/revoke outside scope → 403 `store_scope_forbidden`) — closes the gap where a store-scoped admin could manage any till of the tenant. **`/pair-device` redemption is till-host-only in prod builds** (plan A5: no browser tills; dev exempt) — a browser can no longer burn a one-time code and enroll as a phantom till. | ✅ CODE DONE; ships with the D-P1 deploy (same `manage-devices` redeploy + web deploy). Acceptance: PWA owner login → Tills → create → code appears; enter code on the Mac Electron till (`npm run electron`). |
| **D-W2** | **Sale receipts moved onto the thermal ESC/POS transport** (2026-07-26, ships 0.1.9; closes the D-W1 gap) | D-W1 shipped a proven winspool RAW transport that NOTHING in the sale flow used: `ReceiptDialog`'s Print button called `window.print()`, the OS print path (spooler dialog, browser page formatting, no auto-cut). The transport was exercised only by the setup dialog's test print. **Design decision (user-approved): the SHELL owns transport only, the UI owns layout.** New `hardware:print-raw` IPC + `hardwareController.printRaw(base64)` reuse `printReceipt`'s exact dispatch (network -> configured direct-USB -> system queue; CUPS temp file moved to os.tmpdir() since __dirname is inside the read-only asar when packaged), with a base64-alphabet check and a 512 KB cap on renderer-supplied bytes. The receipt LAYOUT is built in the renderer (`src/services/escpos/`): 48-column/576-dot Font A layout mirroring ThermalReceipt section for section, same `thermalReceipt.*` translation keys, same money arithmetic (extracted to `utils/receiptTotals.ts` and now used by BOTH renderers so the paper and the preview cannot disagree on a cent), CP858 encoding via an explicit character table with diacritic-stripping fallback, and the AT/AEAT QR rastered as a GS v 0 bitmap (~245 dots ~= 30 mm, minimum 4 dots/module) rather than printer-native GS ( k, so it prints on any head. Consequence of the split: every future receipt change (layout, code page, logo, a new country legend) ships as a **UI deploy, no fleet update**. `windowsRawPrint` now flags the delivered-but-unacknowledged case as `outcomeUnknown` on the error object (was message-regex only) and it is propagated through printRaw to the UI. Two defects were caught by rendering a sample receipt to bytes and reading the paper back before any UI wiring: a 4-wide quantity column padded from the LEFT turned **0.482 kg into .482 kg on a fiscal document**, and the QR printed at ~43 mm. Both fixed and pinned by `tests/escposReceipt.test.ts` (7 tests: reset+code-page prologue, cut epilogue, no weighed-quantity truncation, 48-column ceiling, accent bytes never '?', transliteration, QR presence). | ⏳ ACCEPTANCE on the till: **the till must be running 0.1.9 first** - this change spans two artifacts on different pipelines (the ESC/POS layout rides the UI deploy, `printRaw` only exists in the shell), and until the shell is 0.1.9 `canPrintDirect` is false and Print silently falls back to `window.print()`, i.e. looks unchanged. Confirm the version in Settings -> Hardware -> Updates, then print a receipt from the POS after a sale -> paper comes out with no Windows dialog, auto-cut. **Code-page probe must use ã/õ/Á/€, not any accented word:** CP437 and CP850/858 are byte-identical over é á í ó ú ç ñ à â ê ô ü, so those print correctly even if the printer ignores `ESC t 19` and stays on its CP437 default - only ã (0xC6), õ (0xE4), Ã, Õ, Á/Â/À, Í, Ó, Ú and € (0xD5) diverge. If exactly those are garbage the diagnosis is 'ESC t ignored' and `cp1252` will not help either. Check the QR is COMPLETE and scannable as its own item: it is emitted as one ~17.6 KB `GS v 0` block and cheap heads sometimes truncate large single-block rasters (fix would be banding into ~24-64-row slices, UI-only). Receipt-history reprints go through the same `ReceiptDialog` (Transactions.tsx), so the deliberate-reprint recovery path prints thermally too. ⚠️ **Vendus provider output is deliberately EXCLUDED** - when `officialOutput` is provider HTML/PDF the button still uses `window.print()`, because ESC/POS cannot reproduce a provider-rendered document. ⚠️ **Audit-vocabulary gap:** on `outcomeUnknown` the dialog logs NEITHER `POST_SALE_RECEIPT_PRINTED` nor `..._NOT_PRINTED` - both would assert something unknowable - so that (rare) case leaves no fiscal audit event and no resend button; the operator reprints deliberately from receipt history. The event vocabulary has no 'outcome unknown' member; adding one is open work. ⚠️ Residuals: **code page CP858 (ESC t 19) and 80 mm/48 columns are ASSUMPTIONS about the TP80K** - if accents print as noise or lines wrap, it is a one-constant UI-deploy fix (`cp1252`/`columns`), not a shell release, but it has NOT been verified on paper yet; no per-printer code-page setting exists. The shell's old `generateReceiptCommands` stub (no fiscal fields, UTF-8 bytes, 32-char dividers sized for 58 mm paper) is still reachable via the untouched `hardware:print-receipt` IPC and `testPrinter()` - **its UTF-8 encoding is a latent mojibake bug for any non-ASCII text**, masked so far because the built-in test content is pure ASCII. Kitchen/station printing (`printerWorkflowService` -> `testPrinterByName`) still sends plain text and is untouched. |
| **D-W3** | **Cash amount made optional + post-sale receipt auto-print** (2026-07-26, UI-only - no shell bump, rides 0.1.9's `printRaw`) | User asks: (1) a cash sale must not require typing the tendered amount - the Confirm button was disabled until `cashReceived >= total`; (2) on a completed sale, skip the receipt dialog, print automatically when the fiscal backend returned complete data, and open the drawer automatically for cash. ⚠️ **(1) could not be shipped alone without corrupting fiscal records.** `POS.tsx` derived the payment method from the amount - `const paymentMethod = cashReceived > 0 ? 'cash' : 'card'` - so a cash sale confirmed with an empty field would have been recorded as **CARD** (wrong `payment_method` on the transaction and the fiscal document, receipt printing 'Multibanco', and **no drawer kick**, since the auto-open at POS.tsx:1117 keys off the same inference). Fixed by making the operator's actual choice travel with the confirmation: `PaymentDialog.onConfirm` now emits `{ method, cashReceived, change }`. An empty field means exact payment (`cashReceived = total`, `change = 0`); the gate keys on the raw input STRING, not the number, because a typed `0` and an untouched field are both `0` and a typed 0 on a EUR 12.50 sale is a mistake, not exact payment - a typed amount below the total is still blocked. The derived `changeAmount` state was deleted: it was stale inside the confirm closure anyway. Request (2)'s drawer half needed no new code - it was broken by the same inference and works once the method is honest. Auto-print: after the sale, `isFiscalResultPrintable()` gates on issuance completeness - invoiceNo + QR payload always, plus **ATCUD and the four hash characters for PT**, or the **Veri*factu legend for ES** (ATCUD is empty by design there; Spain is detected by legend OR `settings.fiscal.issuer === 'sign_es'`, since the legend is a display string). Only a clean success skips the dialog; `logPostSaleReceiptPrinted` is then called from POS and the audit context left null (setting it with no dialog to mount would dangle - the NOT_PRINTED-on-close path never runs). Every other outcome shows the dialog as before, now with the auto-print result pre-set via `initialPrintNotice` so it explains itself; an **'unknown' outcome shows the warning and NO resend button** and suppresses both audit events. Thermal printing is now one implementation (`services/receiptPrinting.ts`) shared by the auto-print and the dialog's Print button. | ⏳ ACCEPTANCE on the till (needs shell 0.1.9): cash sale with the amount left blank -> Confirm enabled, receipt records **Numerario**, drawer kicks, receipt prints with no dialog; card sale -> no drawer, no cash lines; cash sale with 20.00 typed on a 12.50 total -> change 7.50 on the receipt. ⚠️ **Residual, inherent to the request: auto-print removes the operator's only on-screen view of the receipt.** A printer that is out of paper still reports success on the winspool write, so a blank-paper sale now surfaces NOWHERE - no dialog, no error, and the audit log says PRINTED. Deciding whether that needs a paper-status check (ESC/POS `DLE EOT` real-time status) is open work. ⚠️ Vendus provider-rendered output (`officialOutput` html/pdf_url) is excluded from auto-print and still goes to the dialog. ⚠️ `nextReceiptAfterClose` and `settings.receipt.printDuplicateOnIssue` are both DEAD (never set / zero usages), so no second copy prints on issue - unchanged by this work, but now load-bearing for the assumption that skipping the dialog drops nothing. |
| **D-W4** | **Windows printer setup investigated on the real till over SSH (2026-07-27): the vendor driver is NOT needed, and paper-out is NOT readable** | Goal was to remove two pains: (a) hunting a model+OS-matched vendor driver per till, (b) never printing a fiscal document into an empty printer. Zadig/WinUSB was the assumed answer; it is NOT needed for either. **Findings, measured on the till (HPRT TP80K, `USB\VID_2AAF&PID_6004`, port USB001):** (1) **No vendor driver required.** `Generic / Text Only` was NOT installed, but `Add-PrinterDriver -Name 'Generic / Text Only'` pulled it from the in-box driver store with no download; `Add-Printer -PortName USB001` then succeeded and RAW ESC/POS printed real paper (user-confirmed). The winspool transport uses the RAW datatype, which bypasses driver rendering entirely - so ANY queue on the right port works. ⚠️ The successful run was in an ELEVATED shell; whether `Add-Printer` works unelevated is still UNVERIFIED (the earlier non-elevated attempt failed for a different reason - the driver was simply absent). (2) **Paper-out is unreadable through a print queue.** With the lid physically OPEN and the printer audibly beeping, `Win32_Printer` reported `PrinterStatus=3 (Idle), DetectedErrorState=0 (Unknown)` on all three queues across repeated samples; the same fields correctly report `DetectedErrorState=9` for the HP ENVY on the same machine, so the query is sound and the thermal queues simply carry no status. `ReadPrinter` fails `Win32 6 (invalid handle)` on every queue, with the read inside the open document and with no document at all (`PRINTER_ACCESS_USE` handle, write fails 3003) - so `DLE EOT` real-time status cannot be round-tripped through the spooler either. The remaining non-Zadig avenue (open the `usbprint` device interface `\\?\USB#VID_2AAF&PID_6004#...#{28d78fad-...}` with CreateFile and issue an `IOCTL_USBPRINT_GET_PORT_STATUS`) was written but **never actually executed** - the P/Invoke did not run and the probe was abandoned. **User decision (2026-07-27): drop paper detection.** Rationale: the signal differs per printer model, and printers indicate it physically (the till's beep pattern already distinguishes lid-open from paper-out). Zadig/WinUSB is therefore NOT being pursued - the Windows queue is kept, and the printer stays usable by the rest of Windows. ⚠️ **Correction to D-W1's 'XP-90/XP-80C ghost queues' residual: they are not ghosts.** Both are REAL queues bound to USB001 - the very port the physical TP80K sits on - carrying vendor drivers for different printer models. There is NO queue named for the TP80K on that till at all. Printing works because RAW bypasses the driver, so the model mismatch is invisible. The Printers page showing them as connected was correct; what was missing is that they are duplicates pointing at one device. | ⏳ NEXT (not built): a Hardware -> Printers action that does the setup itself - list USB print devices with their REAL driver binding (`Get-PnpDevice` + `DEVPKEY_Device_Service`, which replaces today's interface-claim guess in the win32 USB scan), show which queues share a port, and offer 'set this up for me' = `Add-PrinterDriver 'Generic / Text Only'` + `Add-Printer` on that port + `setPrinterRole('receipt')`. Needs a shell release. Elevation behaviour must be settled first (see the unverified item above). ⚠️ **OpenSSH Server was enabled on the till (2026-07-27) for this investigation** - key-only, user `pc`, public key `claude-till`. It is a standing remote-shell surface on the machine that signs fiscal documents, reachable from anything on the shop LAN. MUST be removed, or narrowed to key-only + a firewall rule scoped to one workstation, before this till or its image reaches a customer. |
| **D-W5** | **One-click Windows printer setup - no vendor driver hunt** (2026-07-27, ships 0.1.10; implements the D-W4 'next') | New `electron/hardware/windowsPrinterSetup.js`: `listUsbPrintDevices()` reports every USB print device with its **real driver binding** (`DEVPKEY_Device_Service` via the parent USB node - the column Zadig displays), its spooler port, VID/PID, and **which queues share that port**; `setupUsbPrinterQueue()` stages the in-box `Generic / Text Only` driver with `Add-PrinterDriver` (no download, no OS-version matching) and creates a queue on that port, then the controller adopts it as the receipt printer. New IPC `hardware:list-usb-print-devices` / `hardware:setup-usb-printer` (the latter re-broadcasts the printer-config SSOT), preload + `UsbPrintDevice` types, and a **USB printers Windows can see** section on the setup dialog's **USB tab** with a 'Set up for me' button (ONE list: the PnP view and the libusb scan are merged by VID:PID so a printer appears once, with a single action - 'Use this' when a queue named for it exists, 'Set up for me' when it needs one, 'Connect' for a WinUSB-bound device. Moved to the USB tab on user feedback: that is where someone who just plugged a printer in looks, and it is exactly where the old dead end sat - a driver-bound printer showed 'Managed by a Windows print driver - print through its queue: System tab' and offered no way to create that queue. The libusb scan stays below it for the direct-USB path). It also names the two conditions the reference till was in: 'No Windows queue exists for this printer yet' and 'Queues on this port: XP-90, XP-80C - none of them is named for this printer'. Correct because our transport prints with the RAW datatype, which bypasses driver rendering entirely (D-W4). **Every PowerShell path was developed and verified against the real till over SSH**, not written blind: device listing returns the HPRT TP80K (`usbprint`, 2AAF:6004, USB001, queues XP-90+XP-80C); queue creation succeeds; a second run returns `alreadyExisted`; and a name-collision on a DIFFERENT port fails loudly (`A printer named 'XP-90' already exists on port USB001, not USB004`) rather than silently adopting a queue that points elsewhere and routing every receipt to it. Two traps found the hard way and commented in the source: **`$pid` is a read-only automatic variable** - assigning to it throws, and under `ErrorActionPreference=Stop` that kills the enclosing `foreach` and yields an EMPTY list with no error; and `Get-PnpDevice` needs `-ErrorAction SilentlyContinue` for the same reason. | ⏳ ACCEPTANCE (till must be on **0.1.10** first - the UI feature-tests `listUsbPrintDevices`, and on an older shell the whole section renders NOTHING rather than erroring): Settings -> Hardware -> Printers -> Setup -> System tab -> the TP80K should appear under 'Detected USB printers' -> 'Set up for me' -> a queue named `HPRTTP80K` is created and becomes the receipt printer -> print a real receipt through it. Verifiable from the workstation over SSH with `Get-Printer | Where PortName -eq 'USB001'` before/after. ⚠️ **Elevation is UNRESOLVED and handled adaptively.** Every successful `Add-Printer` so far ran in an ELEVATED shell (the SSH session is elevated; `runas /trustlevel` and a `RunLevel Limited` scheduled task both failed to produce a filtered-token test). The app therefore tries unelevated and, on failure, reports the raw Windows error plus 'needs administrator rights' when the message matches a denial stem. ⚠️ That heuristic is **localized-string matching** (the reference till reports in Portuguese) - it can misclassify, so the raw error is always shown too. ⚠️ The 'run the app as administrator' remedy is awkward for a per-user kiosk install; if elevation does turn out to be required, the better answer is a one-time elevated helper at install time, not a prompt during a shift. ⚠️ Nothing removes the duplicate/mislabelled queues (XP-90, XP-80C) - they are only surfaced. |
| **D-W6** | **Settings -> Hardware -> Printers no longer scans on arrival** (2026-07-27, UI-only - no shell bump) | `PrinterManager` (the 'System Printers' block at the top of the printers panel) ran `quickListPrinters()` from a mount effect and, when that resolved, started a **5-second polling monitor**. On the reference till - an old machine - simply navigating to the panel froze the app for the length of a cold enumeration (`main.js` returns the instant cache when warm, so the freeze is the COLD path only and does not reproduce on a warm app). Mount now does no hardware work at all: one button carries both states - **Scan** until a scan has run, **Refresh** afterwards - and `hasScanned` is set in `finally`, on the ATTEMPT rather than on success, so a till with genuinely zero queues (the exact machine D-W5 exists for) does not sit on 'Scan' forever. The two duplicated header blocks were collapsed into one, so the zero-result state gets the same two-state button. Pre-scan copy reads 'Printers have not been checked yet' - the old 'No printers found on this system' would have been a false negative that sends an operator hunting for a hardware fault that is not there. 'Check Status' is hidden until the first scan (nothing to check). | ⚠️ **Hot-plug auto-refresh now depends on a scan having been run.** `PrinterManager` is the ONLY caller of `startMonitoring` in the app, and monitoring moved from mount to the first scan - so `PrinterSetup`'s `onHardwareChange` subscription (auto-rescan of the USB tab when a printer is plugged in) stays silent until someone has clicked Scan at least once. Both subscribers (`PrinterManager`, `PrinterSetup`) live INSIDE this panel and the unmount cleanup already called `stopMonitoring` unconditionally, so the live window narrowed from 'while the panel is open' to 'while the panel is open AND after a Scan' - nothing app-wide lost hot-plug detection. Judged an acceptable trade because the USB tab has its own 'Scan USB' button and the panel is no longer allowed to do background work uninvited, but it IS a behaviour change, not a pure perf fix. ⚠️ **One on-arrival hardware call REMAINS and was deliberately left in**: `PrinterSettingsPanel`'s mount effect calls `getHardwareStatus()`, which on Windows reaches `checkSystemPrinter()` and spawns PowerShell for a single `Get-Printer` (15s timeout). It is one bounded query rather than a full enumeration plus a 5s poll, and it populates the panel's primary connected/type/name readout, which would otherwise read 'Loading...' until clicked - so it was kept. If the panel still feels slow on arrival, this is the remaining candidate and it already has a manual 'Refresh Status' button to fall back on. NOT measured on the till: the ad-hoc SSH access used for D-W4/D-W5 no longer accepts the workstation key. ⏳ ACCEPTANCE: open Settings -> Hardware -> Printers on the till -> it should appear instantly with an empty list and a **Scan** button -> click it -> the list fills and the button becomes **Refresh**. The first click will take exactly as long as the old mount hang did; that cost is now chosen rather than imposed. |
| **D-U5** | **Auto-update channel LIVE (unsigned, config-driven) — residual risks + open ends** (2026-07-24, supersedes D-U2/plan D10) | `electron-updater@6` wired in `electron/updater.js`: inert unless `update_feed_url` is set in config.json (Stage-0 layer); generic HTTPS feed; checks boot+4h; background download; **installs on QUIT only** (never interrupts selling); Windows NSIS only (portable target can't self-update; macOS excluded — needs signing). CI `build.yml` now uploads `latest.yml` + `.blockmap` + `.exe` (the feed set); `package.json` build.publish placeholder exists only so electron-builder emits latest.yml. Runbook: `docs/till-update-runbook.md` §G. Post-review hardening (26-agent adversarial pass, same day): feed is **https-strict** (no LAN-http exception — executables); install moments fixed for the kiosk lifecycle (Windows shutdown never fires 'quit', so quit-only install would defer forever → now installs AT THE BOOT GATE when a download is pending, and gate:restart routes through quitAndInstallIfPending so NSIS and relaunch can't race); `perMachine:false` pinned (per-user install = no UAC on silent update); CI asserts latest.yml exists. ⚠️ Residual: (1) **UNSIGNED** — integrity = HTTPS + latest.yml sha512, no publisher verification; MITM of the feed host = code execution on tills. User accepted "for now"; revisit with U3 signing before fleet scale. (2) **Feed host DECIDED 2026-07-24: Cloudflare R2**, publish AUTOMATED in `build.yml` (main pushes only; installers before latest.yml; latest.yml no-cache; loud ::warning + skip until the four `R2_*` repo secrets exist — setup steps in runbook §G). (3) ✅ **POS-UI nudge SHIPPED 0.1.2 (2026-07-25)**: Settings → Hardware → Updates tile (shell version, live status, "Restart and install now" via new origin-gated `shell:restart-to-install` IPC that tri-states electron-updater's silent-failure modes) + amber "Update ready" chip in the POS status bar; i18n en/pt/es. (4) ✅ **E2E VERIFIED 2026-07-25 on the real Windows till**: 0.1.0 → R2 feed → background download → install-on-quit → relaunched as 0.1.1 (user-confirmed via `electronAPI.shell.version`). 0.1.1 also shipped the CSP dev-origin exception, itself delivered OVER the channel it validated. (5) Tills must be installed per-user (runbook §G note). | ⏳ user: create the R2 bucket + token, add repo secrets `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET`, set tills' `update_feed_url` to the public R2 domain + `/pos/`; then the two-version e2e (v0.1.x→v0.1.y) before relying on it; wire the UI nudge with the next POS-UI pass; revisit signing (U3) before fleet scale-out. |
| **D-I1** | **App-wide i18n sweep — the dev-tool tail is deliberately NOT translated** (2026-07-27) | A report-only sweep of every `.ts`/`.tsx` outside `src/i18n.ts` found user-facing strings that bypass i18n. 317 were confirmed and fixed in round 1; a mechanical re-audit then showed the 45-per-slice cap had also left ~194 real strings in **non-dev** files (`ProfitCosts`, `Reports`, `PrinterSetup`, `ImageUploader`, the contexts…), so a round 3 was run to finish those. What remains is genuinely internal tooling only: `src/components/dialogLabPreviews.tsx` (~86), `src/pages/PlatformConsole.tsx` (~58, zero `t()` calls in the file), `src/components/buttonLab*` (~50), `src/components/DesignSystem2/*` (~130 across Premade2/ColorPalette/Typography/Buttons2/Table2), `CashierTesting`, `ElectronCashierTesting`, `SeedManagement`, `DataSetup`, `DatabaseReset`, `DialogLab`, `PrinterTestPage`, `ReceiptDemo` — roughly 350 strings. Translating internal harnesses is churn a shop owner never sees, so it is deliberately deferred, not missed. ⚠️ Note the round-1 correction: "the tail is all dev-tool" was **wrong** when first written, and only a mechanical count caught it — do not trust a capped agent sweep as a completeness signal. | Re-run the sweep restricted to the dev-tool files with the cap removed, then apply. Only worth doing if these screens are ever put in front of a customer. **Guard:** `npm run check:i18n` fails if any `t(...)` / `<Trans i18nKey>` key in `src/` does not resolve in all three languages — it found two keys already broken before this work (`hr.bookNDays`, `sidebar.openMenu`) that were rendering raw key text on screen. Run it after touching `src/i18n.ts`. |
| **D-I2** | ✅ **`src/pages/Tables.tsx` finished — except one deliberately-skipped pair** (2026-07-27) | The dine-in floor plan had **no `useTranslation` import at all** and no `tables:` namespace. A dedicated uncapped sweep found 80 strings; 75 were converted, including the raw `TableStatus` enum that was printing straight to screen at two sites, and three plural-aware strings now use i18next `count`. **Deliberately NOT converted:** the default table name `Table ${n}` (line ~434) and its parser `table.name.replace(/^Table\s+/i, '')` (line ~232). That string is **persisted as the table's name** and parsed back to draw the floor-plan chip — translating one breaks the chip, translating both changes stored data. Left byte-identical. | To localise the default name properly: store a structural name (or a number) separately from the display label, so nothing has to parse a human-readable prefix back out. Not worth doing for the label alone. |
| **D-I3** | **UI date/number locale is resolved two different ways** (2026-07-27) | `src/utils/locale.ts` (`uiLocale`) was added for HR and maps English to **en-GB** (day-first, matching the rest of a European shop). `Reports.tsx`, `ProfitCosts.tsx` and `Employees.tsx` still use an inline `pt-PT / es-ES / en-US` ternary, so English users see month-first dates there. `Transactions.tsx` and `Tables.tsx` deliberately use `activeProfile().locale` (country profile) instead — that one is **correct by design** and must not be "fixed": a Spanish store must format euros the Spanish way even when a cashier switches the interface to English. | Decide whether English should be day-first everywhere, then point the three inline ternaries at `uiLocale`. Cosmetic; only affects the English UI. |
| **D-MIG1** | **Prod migration ledger verified — several "not deployed" register claims were stale** (2026-07-27) | Asked whether any Supabase migration was pending. Queried the prod ledger directly: `supabase migration list --linked --password "$SUPABASE_DB_PASSWORD"` (the password is in `.env`; without it the CLI dies with `LegacyDbConnectError`, which is what makes this easy to leave unverified). Result on **EasySoft** (`kmojrkkjuehmpordueoe`): **51 local migration files, 51 in the remote ledger, zero pending, zero drift.** Head `20260802000000_platform_admin_control_plane`. This contradicted two rows written from prose rather than from the ledger: **B2** ("Function + `20260716…` migration are committed but **not deployed anywhere**") and **D12→D14** ("⛔ NOT deployed (B2/B3)" for `20260717000000`). Both migrations are applied. Also confirms `20260731000000_product_options` is live, so `products.variants` / `products.modifiers` round-trip through sync. | ✅ VERIFIED + both rows corrected. ⚠️ **Prod only** — staging (`mubdnwmbvdutqzzprjdp`) is still unverifiable from here (its DB password is not in `.env`, B3); `docs/phase2-identity.md` puts staging at `20260712000000`, ~18 behind, unconfirmed. ⚠️ The ledger says **nothing about edge-function deploys** — B2's `pos-checkout` function half stays open. ⚠️ D12→D14's own **post-deploy anon-key + cross-tenant probe checklist has still not been run**: the tenant-scoping migration is applied but not empirically proven on prod. **Rule going forward: query the ledger, don't trust doc prose about deploy state.** |
| **D-SA1** | **System-administrator employee number corrected (ADMIN001 → SYS001)** (2026-07-27, user request — old seeding accident) | The system administrator was seeded as `ADMIN001` while `SYS001` — the number that reads as "system" — was held by an ordinary admin employee (Carlos Silva), with `ADM001` already taken by Maria Santos. Corrected everywhere as a **swap, executed in two ordered phases** (Carlos `SYS001`→`ADM002` first to vacate the number, then the sysadmin `ADMIN001`→`SYS001`); a naive find-replace would have collided mid-flight. Client side is the easy half because `Settings.tsx` and `Employees.tsx` both go through `isSystemAdministrator()` — only `src/utils/systemAdmin.ts` (`DEFAULT_SYSTEM_ADMIN_NUMBERS`) and `seedData.ts:47` (`isStartupSystemAdmin`, a **second, different** rule that also matches on the name "system administrator") encode the number in logic; the rest were comments, fixtures and seed data. 17 occurrences across 13 files updated: `systemAdmin.ts`, `seedData.ts`, `vite-env.d.ts`, `dialogLabPreviews.tsx`, `Settings.tsx`, `Employees.tsx`, `public/bootstrap-data.json`, `public/startup-seed.json`, `public/seed/employees.yml`, `seed/employees.yml`, `bootstrap-admin.cjs`, 4 test files, 2 e2e specs. | ✅ CLIENT + SEED DONE (124 typecheck errors = baseline; 107 tests pass). ✅ **MIGRATION DEPLOYED 2026-07-27** (user-ordered `db push`): `20260803000000_system_admin_employee_number_swap.sql` — NOTICEs confirmed `Phase 1 (SYS001 -> ADM002): 1 renamed, 0 skipped` and `Phase 2 (ADMIN001 -> SYS001): 1 renamed, 0 skipped`; ledger re-verified (52 entries, head 20260803000000, zero pending). renames the live rows. It is ordered (the UNIQUE `employees_tenant_id_employee_number_key` rejects an unordered swap), idempotent, per-tenant, skips any tenant where the target number is occupied, and RAISEs at the end if any `ADMIN001` survives rather than shipping a half-swapped fleet. **Deploy prereq: client and migration must go out together** — the client alone leaves existing installs with no recognised system administrator. Run `supabase db push` when ready (D-MIG1 rule: verify in the ledger afterwards). |
| **D-TS1** | ⚠️ **Store/location scoping of the catalogue — SCOPED, NOT STARTED** (2026-07-27, user request) | User asked that products/categories/inventory "and anything else missed" be scoped by tenant/store/location but **not** by till (two tills in one location must share a catalogue). Investigation splits this three ways. **(a) Already done:** `tenant_id` has been on `products`/`categories` since `20260706000000`, with tenant-scoped sync RPCs in `20260717000000` — both applied on prod (D-MIG1). **(b) Genuinely missing:** there is no `store_id` on the catalogue tables, and this was a **deliberate, documented decision** — `docs/multi-tenant-plan.md:698` reads "devices pull the **whole tenant's** catalog and customer set, not a per-store slice… store-level filtering is deferred, and the schema is ready for it (`store_id` columns exist)". Doing this reverses that decision: migration adding `store_id` + backfill, changes to `get_products_delta`/`upsert_products` and the categories pair, RLS, and every client read. **(c) Missing and unscoped:** `rawMaterials` and `recipeLines` (inventory + recipes) are **local-only Dexie with no tenant OR store column at all** and no Supabase sync (`src/types/rawMaterial.ts`) — they are per-device today, so two tills in one location already disagree. This is almost certainly the "anything else" and is the biggest piece. | 🟡 IN PROGRESS 2026-07-27 (user: "obviously inventory recipes should also be synced to server tables"). Migration **written, NOT yet deployed**: `20260804000000_raw_materials_recipes_server_tables.sql` — creates tenant-scoped `raw_materials` + `recipe_lines` with RLS and the delta/upsert sync pair mirroring the products pattern (SECURITY DEFINER, tenant from `app.tenant_id()` never the payload, REVOKE PUBLIC/anon). ⚠️ **Client sync wiring is NOT done** — `syncManager` has no entity entry for either table yet, so nothing pushes/pulls until that lands; deploy the migration and the client together. Store scoping still needs the user's call on: reversing the plan-§698 deferral; whether inventory/recipes get promoted to synced server tables (a new table + RLS + sync pair, not a column add); and whether "store" means `stores.id` or a coarser location concept. Also unresolved: what a till pulls when its device row has no `store_id`. |
| **D-FR1** | ✅ **Non-fiscal fallback receipt — IMPLEMENTED** (2026-07-27) | Legal basis in **`docs/fiscal-fallback-legal-brief.md`**: **Ofício-Circulado 30213 (2019-10-01)** makes this a named path — on inoperability the entity issues on AT-authorised tipografia pre-printed documents, afterwards recovered into the certified program. | Shipped: `src/fiscal/fiscalFailure.ts` (dispatch classification), `src/fiscal/nonFiscalFallback.ts` (eligibility + slip issuance), `createNonFiscalFallbackAtomic` in `localDatabase.ts` (transaction + items + audit event in one Dexie txn, **no fiscal_documents row** → naturally excluded from SAF-T and from every série counter), `NonFiscalFallbackDialog` (operator confirmation), `ManualDocumentDialog` + the fiscal-log reminder (recording the paper document, which clears it). Slip reference `TNF-<till>-<n>` from its own per-till counter — never a fatura number. The three user answers are settled: (1) own counter, not the fiscal series; (2) NO re-issue online on reconnect — the paper invoice IS the invoice; (3) the sale completes, but only behind an explicit operator confirmation. `receiptPrinting.ts:62`'s never-auto-print policy is kept as-is: the slip goes to the dialog so the operator sees what they are handing over. |
| **D-FR2** | 🟡 **Retry double-issue — FIXED for Vendus, mitigated for InvoiceXpress** (2026-07-28) | Vendus and InvoiceXpress minted a fresh `attemptId` (and so a fresh `tx_id` / `reference`) on every call, so retrying an unresolved sale could create a SECOND document. Both now reuse the open attempt's id for an identical sale via `src/fiscal/issueAttemptReuse.ts`, mirroring the fiskaly pattern. | **Vendus is genuinely fixed**: `tx_id` is a documented server-side guarantee — *"this will ensure that only a document may be created using the same tx_id, even if multiple requests are made by mistake"* ([documents.doc](https://www.vendus.pt/ws/v1.1/documents.doc)) — so a retry replays the original document. Retry is offered again on an unresolved failure for Vendus and fiskaly (`retryIsIdempotent`). ⚠️ **InvoiceXpress is only mitigated**: it documents NO idempotency (`proprietary_uid` is described as a client-side correlation field, not an enforced key), so reuse buys identity and detectability, not prevention. Retry stays withheld there and the fallback keeps its attestation gate. To finish: add a `find_document` action to the `invoicexpress-fiscal` edge function that looks the sale up by reference before re-sending, and adopt the existing document if found — blocked on confirming the list endpoint can filter by reference (the public docs hide 14 of its query parameters). |
| **D-FR3** | ✅ **Provider 4xx fast path — IMPLEMENTED AND DEPLOYED** (2026-07-28) | A definitive provider 4xx proves no document was created and should not need the operator to check the backoffice, but the edge functions collapsed every upstream failure into an opaque string. | Both `vendus-fiscal` and `invoicexpress-fiscal` now throw a typed `UpstreamError` from their fetch wrapper and answer `{error, providerStatus, dispatched:true}` at HTTP 200, so supabase-js delivers the body instead of a generic non-2xx. `fiscalFailure.ts` gained a third dispatch state, `rejected`, which skips the attestation. Statuses 408/409/425/429 are excluded — each can coexist with a created document. Our OWN faults (missing API key, failed guards) still answer 500 with no `providerStatus`, so a misconfigured till never offers a paper invoice for a config bug. InvoiceXpress creates then finalizes, so an `UpstreamError` after the create is downgraded to a plain error — past that point the document exists and a 4xx is not proof of absence. **DEPLOYED to prod 2026-07-28** — `vendus-fiscal` and `invoicexpress-fiscal` both at version 6; verified by round-trip (`supabase functions download` produced no diff against HEAD). A missing `providerStatus` still classifies as `unresolved`, so the degradation path stays safe. |
| **D-FR4** | ⚠️ **The manual-invoice reminder is per-device** (2026-07-27) | `fiscalAuditEvents` is local Dexie and is not synced, so a slip issued on till A never appears in the reminder on till B. In a two-till store the operator must reconcile on the till that issued the slip. Slip references carry a till discriminator (`TNF-<deviceId tail>-`) so the two counters cannot collide. | Sync `NON_FISCAL_FALLBACK_ISSUED` / `MANUAL_DOCUMENT_RECORDED` to a tenant-scoped server table and derive the reminder from the union. |
| **D-FR5** | ⚠️ **Spain (SIGN ES) deliberately has no fallback** (2026-07-27) | Veri*factu has its own contingency route (records generated locally and remitted afterwards); routing a Spanish sale through the Portuguese paper-book flow would be wrong in both jurisdictions. `isNonFiscalFallbackProvider` excludes `sign_es`, so a Spanish sale still blocks on failure exactly as before. | Scope the Veri*factu deferred-remission flow separately if Spain goes live. |